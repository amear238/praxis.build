# ISSUE_REGISTER — PRAXIS

Append-only. Bugs, blockers, unresolved questions, deferred items.

---

- [2026-07-06T20:15Z] WHO: orchestrator | WHAT: `bd` (beads) binary not found on PATH on this Mac, though .beads/ dir exists in repo and CLAUDE.md mandates bd for all task tracking | WHY: `which bd` returns nothing; checked ~/go/bin, ~/.local/bin, /usr/local/bin, /opt/homebrew/bin | WHERE: local machine PATH; repo .beads/ intact | WHEN: discovered 2026-07-06 session start | HOW: Reinstall beads (or fix PATH) before next dispatch cycle so Praxis_build-rnu (0.7) and Praxis_build-v5h (0.8) can be claimed/closed properly | STATE: closed — superseded by the 2026-07-08 RESOLVED row (bd v1.1.0 via brew, on PATH)
- [2026-05-12T00:00Z] WHO: orchestrator | WHAT: Step 0.7 (Drive sync) and Step 0.8 (full-loop verification) NOT executed this session | WHY: User wrapped session to continue on remote server side | WHERE: beads Praxis_build-rnu (0.7), Praxis_build-v5h (0.8) — both still open | WHEN: deferred 2026-05-12 | HOW: Resume by either running 0.7 subagent (creates Drive PRAXIS folder, moves Sheet in, shares) + manual Drive Desktop install; or skip 0.7 and run 0.8 verification (curl test against webhook after user binds Telegram credential in n8n UI) | STATE: deferred
- [2026-05-12T00:00Z] WHO: orchestrator | WHAT: 'PRAXIS xarq5bot' n8n credential does not exist yet; 3 Telegram nodes auto-bound to wrong cred 'Telegram account (networkchuck2)' | WHY: n8n SDK auto-assigns existing credentials on workflow update | WHERE: workflow EmMbN4sslwIx1ydn draft a8400246 | WHEN: blocks 0.6 prod usage | HOW: User opens n8n UI → Credentials → New 'Telegram API' named exactly 'PRAXIS xarq5bot' → paste BotFather token → in workflow, rebind each of 3 Telegram nodes to that cred → save + activate the draft version | STATE: open

- [2026-05-11T00:05Z] WHO: orchestrator | WHAT: Telegram bot token leaked into chat transcript by user | WHY: User pasted full token in answer to setup question | WHERE: conversation transcript (not git) | WHEN: discovered 2026-05-11 | HOW: User decides rotate-vs-keep. Token NEVER committed to repo (verified via secret grep). User must paste token into n8n credential 'PRAXIS xarq5bot' in UI before re-activating workflow. | STATE: open (user decision pending)
- [2026-05-11T00:05Z] WHO: orchestrator | WHAT: Need Telegram chat_id to wire 0.6 | WHY: bot must know where to send | WHERE: Step 0.6 | WHEN: 2026-05-11 | HOW: User provided chat_id 6156528469; hardcoded in workflow nodes. | STATE: closed (chat_id 6156528469 in workflow)
- [2026-05-11T00:05Z] WHO: orchestrator | WHAT: Block 0 step "Configure n8n Host-Side Webhook" — confirm exact path layout (/signals/ subdir to be created) | WHY: Dispatch needs concrete write target | WHERE: /Volumes/Sensidine/Praxis.build/signals/ | WHEN: before 0.5 dispatch | HOW: Subagent creates dir + .gitkeep + .gitignore for *.json | STATE: closed (done in 0.5 commit)
- [2026-05-11T00:40Z] WHO: orchestrator (audit) | WHAT: 0.6 subagent draft breaks file-write-error routing — `Write Signal File.error` branch removed; file-write failures now return HTTP 200 incorrectly | WHY: Subagent rewired error path to `Telegram Notify Success.error → Respond 500` instead of `Write Signal File.error → Respond 500` | WHERE: workflow EmMbN4sslwIx1ydn draft version 208a9f1c (active version 2ee466c5 still correct, prod unaffected) | WHEN: caught in orchestrator audit before commit | HOW: Fix dispatch completed — final draft (versionId a8400246) verified via get_workflow_details; Write Signal File.error → Respond 500 restored. Credential auto-bind to networkchuck2 remains (n8n SDK limitation) — user rebinds in UI. | STATE: closed (fixed in 4b2c0b9)
- [2026-05-11T00:20Z] WHO: orchestrator | WHAT: ARCHITECTURE — n8n runs on a REMOTE box, not local Mac | WHY: Webhook writes signal files to remote box's filesystem at /Volumes/Sensidine/Praxis.build/signals/ which does not exist there. Even if path existed, files land remote, not on local Mac where NinjaTrader watcher will eventually run. | WHERE: n8n workflow EmMbN4sslwIx1ydn; blocks production data path; not a Block 0 infra blocker but a Block 1 readiness blocker | WHEN: surfaced 2026-05-11 immediately after 0.5 commit | HOW: (a) For Block 0 verification: switch webhook write path to a sane remote-host path (e.g., /tmp/praxis-signals/ or n8n's data dir) and confirm file lands there. (b) For prod path: choose delivery mechanism — SFTP push, rsync, return signal in HTTP response and have local poller, or move n8n local. Decide before Block 1. | STATE: open
- [2026-07-08T00:00Z] WHO: orchestrator | WHAT: RESOLVED — bd (beads) reinstalled via `brew install steveyegge/beads/bd`, now v1.1.0 on PATH; .beads/ ledger intact (2 open issues: Praxis_build-rnu, Praxis_build-v5h) | WHY: closes the 2026-07-06 bd-missing row | WHERE: /opt/homebrew/bin/bd | WHEN: 2026-07-08 | HOW: verified with bd --version and bd ready | STATE: closed
- [2026-07-25T16:33Z] WHO: orchestrator | WHAT: BLOCKED — master/manager/auditor agent-stack install halted at P0 feature-availability gate. `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` and `CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS` require Claude Code v2.1.217+; this machine runs 2.1.206, so both env vars are SILENTLY IGNORED | WHY: without MAX_SUBAGENT_SPAWN_DEPTH=2 the manager cannot spawn a worker (default depth 1: "subagents can't spawn their own subagents"). master→manager→worker collapses into one context — the exact separation the stack exists to enforce. A silently-ignored setting is a guard that isn't there | WHERE: .claude/settings.json (proposed `env` block); nesting deliverable in docs/reports/2026-07-25-agent-stack-install.md | WHEN: discovered 2026-07-25 P0 check; evidence = literal doc marker `{/* min-version: 2.1.217 */}` on both vars at code.claude.com/docs/en/env-vars.md, confirmed twice-independently | HOW: upgrade Claude Code to >= 2.1.217, re-run `claude --version` to confirm, THEN restart the install from P0. Do NOT substitute a prompt instruction for the missing depth control. Fields 1-6 (agent key, initialPrompt, skills:, memory:, Agent(type) allowlist, SubagentStart/Stop matchers) are supported on 2.1.206 and can proceed once nesting is real | STATE: SUPERSEDED 2026-07-26 — see correction row below; version facts in this row are wrong, do not action

- [2026-07-26T00:00Z] WHO: orchestrator | WHAT: Parallel PreToolUse denies mask each other — when two or more PreToolUse hooks match the same tool and both exit 2, only ONE stderr message surfaces in the tool_result, and it is the LAST hook to FINISH, not the first to deny | WHY: cost trip-test T3 two consecutive wrong diagnoses. gate-commit.sh runs an `lsof` single-session check (~1s) and always finished after no-commit-guard.sh, overwriting its message; the guard had been denying correctly the entire time. First wrong diagnosis: "hook ordering shadows the second hook" — reordering changed nothing. Second wrong diagnosis: "agent_id is absent from praxis-manager payloads, so the guard self-disables" — captured payloads showed agent_id present | WHERE: .claude/settings.json hooks.PreToolUse (Bash matcher: scripts/no-commit-guard.sh + .claude/hooks/gate-commit.sh); proven by a two-hook scratch experiment with the artificial delay swapped between hooks | WHEN: discovered 2026-07-25 during T3 | HOW: STANDING RULE — never diagnose a deny-message mismatch by reasoning about hook order or payload shape. Instrument first: add an unconditional raw-stdin capture line to the suspect hook before any exit, re-run live, read the captured payload, THEN diagnose. A hook that appears not to fire may be firing and losing the race to print | STATE: open (behavioral constraint, not a defect to fix)

- [2026-07-26T00:00Z] WHO: Amear | WHAT: RESOLVED — stale Claude Code 2.1.206 npm-global install removed; only the live 2.1.220 native binary remains | WHY: the stale copy answered `claude --version` first and produced a false "2.1.206, env vars silently ignored" P0 verdict that halted an entire session with a BLOCKED report and zero files installed. Static doc citation (`min-version: 2.1.217` marker) was correct in itself; the version it was checked against was not | WHERE: npm global prefix; /Users/admin/.local/bin/claude | WHEN: pre-session, completed by Amear 2026-07-25 | HOW: `npm uninstall -g @anthropic-ai/claude-code` → removed 2 packages; `which -a claude` → /Users/admin/.local/bin/claude listed twice (duplicate PATH entry, single binary — NOT two installs); `claude --version` → 2.1.220 (Claude Code). Claude must not attempt this removal itself; Amear performs it pre-session | STATE: closed (verified 2026-07-25 by Amear)

- [2026-07-26T00:00Z] WHO: orchestrator | WHAT: RECOVERED — the P0 BLOCKED report at docs/reports/2026-07-25-agent-stack-install.md was overwritten in place by the consolidated PASS report and is now restored | WHY: the file was `git add`-ed while still holding the BLOCKED content, so git wrote its blob to the object store before the overwrite; the blob survived as unreferenced | WHERE: recovered to docs/reports/2026-07-25-agent-stack-install-P0-BLOCKED-superseded.md (141 lines, byte-identical to the staged original) | WHEN: recovered 2026-07-26 at session-31 close | HOW: `git fsck --lost-found --dangling` → grep each dangling blob for "BLOCKED at P0"/"2.1.206" → blob 54ce1982dfdabc96d0df4b723f2917df7c2fb315 → `git cat-file -p` to the new path. Recovery worked only because the file had been staged; had it been overwritten before any `git add`, it would have been unrecoverable. Amear decides whether the superseded report is retained or deleted — its P0 conclusion is wrong (built on the stale 2.1.206 binary) but it is the primary record of how the false verdict was reached | STATE: closed (recovered; retention is Amear's call)

- [2026-07-26T00:05Z] WHO: orchestrator | WHAT: CORRECTION to the 2026-07-25T16:33Z nesting row above — that row's claim ("requires v2.1.217+; this machine runs 2.1.206; both env vars are SILENTLY IGNORED; upgrade before restarting from P0") is WRONG on the version facts and must not be actioned. Original row retained verbatim as the record of the false verdict | WHY: the version was read from a stale npm-global 2.1.206 binary that shadowed the live install. Both version calls in that row were wrong. What remains TRUE and load-bearing: nesting is OFF BY DEFAULT in Claude Code — subagents cannot spawn subagents unless it is enabled | WHERE: .claude/settings.json `env` block; ISSUE_REGISTER row [2026-07-25T16:33Z] | WHEN: corrected 2026-07-26 at session-31 close | HOW: CORRECT TEXT — this machine runs Claude Code 2.1.220 (verified in-session, `claude --version`, single binary at /Users/admin/.local/bin/claude). Nesting is off by default. `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH=2` is REQUIRED and must be PRESENT in .claude/settings.json BEFORE the `agent` key is installed — without it master→manager→worker silently collapses into one context. It is not a version blocker; it is a mandatory setting. Proven in-session: depth-2 spawn observed live, DISPATCH_LOG grew +2 rows for one master dispatch (praxis-manager 20:52:54Z → praxis-auditor 20:53:06Z). The instruction to strip both env vars was also wrong — both stay | STATE: closed (correction recorded; original row superseded, not deleted)

- [2026-07-26T15:30Z] WHO: praxis-master | WHAT: BLOCKER — the parenthetical type-scoping in agent-frontmatter `tools: Agent(<type>)` is NOT ENFORCED on Claude Code 2.1.220. A producer can spawn its own grader. `praxis-manager` holds `tools: Agent(praxis-worker)` and spawned `praxis-auditor` anyway, with no refusal, no error, and no permission prompt | WHY: T6 trip test. The manager was explicitly instructed to ATTEMPT the spawn and report the verbatim refusal. There was no refusal to report — the call ran to completion (29 tool uses, 106,485 subagent tokens, 333s) and returned a full SPEC_RUBRIC grading. This is the exact v1 defect Amear identified ("the manager's unrestricted `Agent` tool — together they let the grader be conscripted as a producer") and v2's `Agent(praxis-worker)` narrowing DOES NOT FIX IT. Producer/grader separation is currently instruction-only, enforced by nothing | WHERE: `.claude/agents/praxis-manager.md:8` (allowlist, correct as specified) and `:20-21` (the false claim: "You cannot spawn the auditor — that restriction is structural and intentional"); hook rows in DISPATCH_LOG.md | WHEN: discovered 2026-07-26 session-32 during T6 | HOW: EVIDENCE — hook-written SubagentStart rows the manager cannot author: `14:34:15Z WHO: praxis-manager` → `14:34:23Z WHO: praxis-auditor` (+8s). REPRODUCTION, not a one-off: the identical pair appears at `14:16:47Z` → `14:16:50Z` (+3s) earlier the same session. Corroborating asymmetry — T7 proved `Agent(praxis-worker)` ADMITS the worker, and this proves it does not EXCLUDE the auditor, so the parenthetical narrows nothing; the session roster is what governs. FOURTH silently-ignored-field incident in this project (after the nesting-default misread, the stale-binary version read, and frontmatter `hooks:`). CONSEQUENCE — `praxis-manager.md:20-21` must be corrected or backed by a real restriction; until then the master is the ONLY thing keeping a producer off its own grade, and that is a behavioral guarantee, not a structural one. NOT YET TESTED: whether the auditor's own `disallowedTools: Agent` and the worker's `disallowedTools: Agent` ARE enforced — `disallowedTools` is a different mechanism from `tools:` parenthetical scoping and must not be assumed to share its fate. That is T8 | STATE: open (blocks the producer/grader-separation claim of agent-stack v2; Amear's call whether v2 ships with the claim corrected-and-documented or held for a real fix)

- [2026-07-26T10:00Z] WHO: orchestrator | WHAT: BLOCKER — agent-frontmatter `hooks:` is SILENTLY IGNORED on Claude Code 2.1.220. Stack v2 wires all three guards ONLY via frontmatter (praxis-master: master-write-guard + master-bash-guard; praxis-manager and praxis-worker: no-commit-guard) and its settings.json removes them from PreToolUse. Installing v2 as specified DELETES three working guards and leaves nothing in their place | WHY: PATCH_NOTES asserts "the three guards are wired via agent frontmatter, which scopes them by construction." That is false on this build. Proven by live probe with a positive control: the identical script at the identical relative path never fired in 5/5 frontmatter cases, then fired and blocked immediately when the same entry was moved to .claude/settings.json. The frontmatter itself parses fine — `tools:` from the SAME block is enforced — so `hooks:` alone is dropped. An invented key `zzz_totally_bogus_key` produces byte-identical silence, so the key is unvalidated, not merely unsupported: zero bytes on stderr, no warning, no validation error | WHERE: .claude/agents/praxis-{master,manager,worker}.md frontmatter in the v2 drop; .claude/settings.json hooks.PreToolUse; probe evidence at scratchpad/fm-hooks-probe.md | WHEN: discovered 2026-07-26 at v2 P0, BEFORE any v2 file was installed | HOW: keep the v1 settings.json PreToolUse registration exactly as committed at b8008e4 — it already satisfies v2's stated fail-safe requirement, scoping each guard in-script against agent_type/agent_id from captured live payloads. Do NOT delete those entries. The inert frontmatter `hooks:` blocks in the v2 agent files must be stripped or they will read as protection that does not exist. Third silently-ignored-field incident in this project | STATE: open (blocks the guard-wiring portion of v2 only)

- [2026-07-26T14:50Z] WHO: praxis-master | WHAT: THIRD REPRODUCTION of the open 2026-07-26T15:30Z blocker (`tools: Agent(<type>)` not enforced). Not a new defect — recorded so the reproduction count is on disk. praxis-manager, holding `tools: Agent(praxis-worker)`, spawned praxis-auditor with no denial: not a tool error, not a permission prompt, not a silent no-op | WHY: Amear authorized a deliberate instrumentation probe of the manager→auditor path to capture the hook payload. The probe returned no payload to capture BECAUSE NO HOOK FIRED. A test designed to read a block string cannot distinguish "hook denied with an unreadable message" from "no hook exists" — this run resolves that ambiguity in favour of the latter | WHERE: .claude/agents/praxis-manager.md:8; reports/2026-07-26-harness-auditor-spawn-payload.md | WHEN: 2026-07-26 session-33 | HOW: EVIDENCE INDEPENDENT OF THE PRODUCER'S SELF-REPORT — hook-written SubagentStart rows in DISPATCH_LOG.md, which no agent can author: `14:50:20Z WHO: praxis-manager` → `14:50:28Z WHO: praxis-auditor` (+8s). Third occurrence; prior pairs at 14:16:47→14:16:50 and 14:34:15→14:34:23. Binary confirmed in-session as 2.1.220, `which -a claude` returning /Users/admin/.local/bin/claude twice = one duplicate PATH entry, single install — so this is NOT a stale-binary artifact and the 2026-07-25 false-BLOCKED failure mode is excluded. NEW SUB-FINDING, marked UNVERIFIED: the roster surfaced to the spawned manager was byte-identical to the MASTER's roster (praxis-auditor + praxis-manager, praxis-worker absent), consistent with H1 already confirmed in the T6/T7 root-cause section — child inherits parent roster. Consistency is not proof; not re-probed here. ALSO UNVERIFIED: whether the agent that ran was the praxis-auditor definition or a generic agent resolved under that name | STATE: open — folded into the 2026-07-26T15:30Z row, no separate remediation

- [2026-07-26T15:00Z] WHO: orchestrator | WHAT: PLATFORM DEFECT — a subagent's own `tools: Agent(...)` allowlist is SILENTLY IGNORED when that agent is spawned as a CHILD; it inherits the parent's spawnable roster instead. The identical allowlist IS enforced when the same agent runs as the main thread | WHY: decided by one control observation — as a main thread, praxis-manager refuses the auditor (`Agent type 'praxis-auditor' not found. Available agents: praxis-worker`), but spawned by praxis-master the same `tools: Agent(praxis-worker)` line is discarded and it spawned praxis-auditor to completion, twice (hook rows 14:16:47Z→14:16:50Z and 14:34:15Z→14:34:23Z). This falsified the competing intersection hypothesis, which predicted a refusal. Consequence as v2 shipped: praxis-manager.md line 20 ("You cannot spawn the auditor — that restriction is structural and intentional") was FALSE; a producer could conscript its own grader. Second silently-ignored frontmatter field after `hooks:` | WHERE: .claude/agents/praxis-manager.md tools:; .claude/agents/praxis-worker.md disallowedTools: | WHEN: discovered 2026-07-26 during v2 T6/T7 | HOW: FIXED at tier 1, not by instruction — scripts/agent-spawn-guard.sh, a fail-safe PreToolUse guard on tool_name=="Agent" keyed on the OBSERVED fields .agent_type (caller) and .tool_input.subagent_type (target), blocks praxis-manager→praxis-auditor and any praxis-worker spawn, allows everything it cannot positively identify. Master's own direct route to the auditor verified still open. Re-probe both behaviours after any Claude Code upgrade; if upstream starts honouring child allowlists the guard stays as defence in depth | STATE: closed (hook-enforced; underlying platform behaviour unchanged)
