# B1-f Consumer Tests T1–T3 — Mac-side Run (bead Praxis_build-ct5)

**Date:** 2026-07-14, 15:56–16:10 ET (19:56–20:10 UTC) · **Operator:** Mac orchestrator agent (headless — no NT8 GUI access)
**Consumer:** PraxisSignalConsumer.cs sha256 `e4581f4a…` (deployed copy `~/praxis-signals/PraxisSignalConsumer.cs` = repo `ninjascript/PraxisSignalConsumer.cs`, identical git blob `9c7cd785`), STARTED 15:23:01 ET on NT8 Sim101, chart NQ 09-26 (per session-10 HANDOFF), Parallels Win11 VM watching `\\Mac\praxis-signals` = `~/praxis-signals`.

## Result summary

| Test | Result | One-liner |
|---|---|---|
| T3 / liveness probe (malformed JSON → rejected, no order) | **PASS** (caveat §5) | 34-byte truncated JSON moved to `rejected/` in ≤6 s; no journal line (per design); no order possible (journal untouched) |
| T1 (valid signal → exactly one bracket order) | **FAIL / BLOCKED** | Schema-perfect payload REJECTED in ≤1–2 s, **three independent attempts** (drop-path, symbol, and content-age variations); zero journal lines ⇒ rejected pre-dedup, no order submitted |
| T2 (dedupe, bead 9tl) | **NOT RUN** | Requires an ACCEPTED T1 first |
| T4 (NT8 restart → no replay) | **NOT RUN** | Trader-touch; blocked behind T1 |

**Bottom line:** the consumer is alive and moving files (T3 proves the watch→disposition pipeline), but it deterministically rejects every schema-valid signal. The reject *reason* string is only printed to NT8 Output/Log inside the VM — not observable from the Mac. Zero orders were placed (journal has zero ACCEPTED lines; order submission is unreachable without one).

## 1. Preconditions (verified 15:56 ET)

- Journal `~/praxis-signals/praxis-processed-signals.log`: 3 bytes = UTF-8 BOM only (created 15:23:01 by the VM-side `StreamWriter(FileMode.Append, Encoding.UTF8)` — proves the consumer holds the journal open).
- `processed/`, `processed/duplicates/`, `rejected/` empty, all created 15:23:01. `incoming/` empty.
- Deployed .cs verified: `shasum -a 256` → `e4581f4ad2f03d8f22594be9ca03d4565e09dc539282b19f88d856f82fbb4e9a`; `git hash-object` of deployed copy and `ninjascript/PraxisSignalConsumer.cs` both `9c7cd785a39b064f91fac8154c415cbd2a01942e`.
- Accepted schema derived from source (NOT the stale template, bead 6h7): required `signal_id`, `symbol` (must equal `Instrument.MasterInstrument.Name`, e.g. "NQ"), `side` (BUY|SELL), `qty` (positive int ≤ MaxQty 5), `price` (>0 finite), `ts` (ISO-parseable); optional `stop`/`target`. Flat JSON only. Rejected files are **NOT journaled** (source comment, journal region): only ACCEPTED/DUPLICATE write journal lines.
- Drop-path note: the consumer watches the signals **root** (`SignalsPath`), not `incoming/`. The Mac 1 s sweep daemon promotes `incoming/*.json → root` (mv, same fs). Both paths were exercised below.

## 2. T3 / liveness probe — PASS

**Dropped** (atomic `.tmp` → `mv` into `incoming/`) at **19:56:55Z**, filename `2026-07-14T19-57-00.000Z-SIM-B1F-T3-BROKEN-20260714.json`, 34 bytes, contents (truncated JSON, verbatim):

```
{"symbol":"NQ","side":"BUY","qty":
```

**Observed:** first poll at **19:57:01Z** — `incoming/` already empty, file present in `rejected/`:

```
-rw-r--r--@ 1 admin staff 34 Jul 14 15:56:55 2026 rejected/2026-07-14T19-57-00.000Z-SIM-B1F-T3-BROKEN-20260714.json
```

Journal unchanged (3 bytes, `xxd` = `efbbbf` only). **PASS**: malformed → `rejected/`, no journal entry (matches source: rejects are not journaled — the runbook/task expectation of a "journal line" for rejects does not match the code and is noted as a docs nit), no order.

## 3. T1 — FAIL / BLOCKED (three attempts, all instantly rejected)

### Attempt 1 — via `incoming/` (sweep-promoted), 19:57:16Z
File `2026-07-14T19-57-20.000Z-SIM-B1F-T1-20260714-155720.json` (126 B), contents verbatim:

```
{"symbol":"NQ","side":"BUY","qty":1,"price":29874.00,"signal_id":"SIM-B1F-T1-20260714-155720","ts":"2026-07-14T19:57:20.000Z"}
```

Rejected ≤1 s: `rejected/` dir mtime **15:57:17** vs drop 15:57:16 (`stat -f %Sm`). File in `rejected/` byte-identical to what was dropped (xxd-verified — no share-side corruption). Journal unchanged.

### Attempt 2 — direct root drop per runbook convention (`.tmp` → `mv` inside `~/praxis-signals/`), 20:00:16Z
File `2026-07-14T20-03-00.000Z-SIM-B1F-T1-20260714-160300.json` (126 B), same payload with `signal_id=SIM-B1F-T1-20260714-160300`, `ts=2026-07-14T20:03:00.000Z`. Poll at **20:00:23Z**: already in `rejected/`. Journal unchanged.

### Attempt 3 — aged-content variant (rule out share visibility race), 20:03:37Z
Payload written to `.t1c.tmp` at 20:03:17Z, `sync`, aged 20 s, then renamed to `2026-07-14T20-10-00.000Z-SIM-B1F-T1-20260714-161000.json` (`signal_id=SIM-B1F-T1-20260714-161000`). Rejected by next poll (20:05:59Z shows it in `rejected/`; mtime preserved 16:03:17). Journal unchanged.

**Verbatim final state:**

```
rejected/2026-07-14T19-57-00.000Z-SIM-B1F-T3-BROKEN-20260714.json      34 B  15:56:55
rejected/2026-07-14T19-57-20.000Z-SIM-B1F-T1-20260714-155720.json     126 B  15:57:16
rejected/2026-07-14T20-03-00.000Z-SIM-B1F-T1-20260714-160300.json     126 B  16:00:16
rejected/2026-07-14T20-06-00.000Z-SIM-B1F-DIAG-MNQ-20260714-160600.json 133 B 16:02:01   (diagnostic, §4)
rejected/2026-07-14T20-10-00.000Z-SIM-B1F-T1-20260714-161000.json     126 B  16:03:17
processed/            : empty (duplicates/ empty)
praxis-processed-signals.log : 3 bytes (BOM only) — ZERO journal lines all session
```

## 4. Diagnostic probe — symbol-mismatch hypothesis REFUTED

Given the 07-14 VM blocked report (chart was MNQ pre-fix), one probe with `"symbol":"MNQ"` was dropped at 20:02:01Z (`signal_id=SIM-B1F-DIAG-MNQ-20260714-160600`, qty 1, Sim101 — sim-authorized diagnostic; would have fired ONE micro bracket if accepted). **Also rejected ≤7 s.** So the rejection is payload-independent across symbol NQ/MNQ — NOT the simple "chart still on MNQ" failure mode.

## 5. Analysis — where the rejection can and cannot be

Eliminated by evidence:
- Payload schema (byte-verified against ValidateSignal in sha e4581f4a — every check passes on paper for the T1 payloads).
- Drop path (incoming-sweep vs direct root mv), content age (0 s vs 20 s), symbol (NQ vs MNQ), file corruption (rejected copies byte-identical).
- Mac-side daemons as the mover: sweep only promotes; backlog/stale checks only alert. Only the VM consumer knows `rejected/`.
- Order-submission failure path: that path journals ACCEPTED before moving to rejected/; journal has zero lines.

Therefore every file was rejected **pre-dedup** — in `ProcessFile` at the read, parse, or validate stage. Leading hypothesis: **VM-side `File.ReadAllText` over the Parallels share returns empty/partial content (→ "malformed JSON") or throws IOException repeatedly (5 rapid FSW events can burn `MaxReadAttempts=5` → "unreadable after 5 attempts")**, rejecting every file regardless of content. Caveat on T3: under this hypothesis T3's PASS is vacuous (any file would reject); T3 is only confirmed non-vacuous once a valid file is ACCEPTED.

The discriminating fact — the verbatim `PRAXIS-B1f … REJECTED (file '…') — reason` lines — exists only in NT8 Output/Log/trace inside the VM. Request brief filed at `~/praxis-signals/BRIEF-2026-07-14-reject-reason-request.md` (VM coworker, read-only ask, expected reply `b1f-reject-reasons-2026-07-14.md`); no reply within this session's 180 s wait window.

## 6. Anomalies

1. **Every schema-valid signal deterministically rejected in ~1–2 s with zero journal writes** — primary blocker, root cause VM-side-only (§5).
2. Rejects are not journaled by design; the install runbook's T3 wording and the test tasking both imply a reject journal line — docs mismatch (relates to bead 6h7 stale-template cluster).
3. Runbook example price 29874.00 vs actual NQ ~20125.5 on 07-09 (archived B1-b payload) — payload price is only used for bracket-relative sanity so it cannot cause these rejects, but the runbook figure looks fictional; nit.
4. Consumer watches the signals ROOT, while ops guidance says "drop into incoming/" — works only because the 1 s sweep promotes; worth documenting explicitly.

## 7. What remains

- **Trader-touch / VM:** read NT8 Output or `Documents\NinjaTrader 8\log`+`trace` for the `PRAXIS-B1f … REJECTED` reason lines (brief already in the share mailbox); fix the named cause; then re-run T1 (fresh signal_id), T2 (same signal_id, new ts/filename → DUPLICATE journal line + `processed/duplicates/`, no second order), and T3 non-vacuous confirmation.
- **T4 (trader-touch):** NT8 restart → startup line reports N journaled signal_ids loaded, no replay; optional third-ts redrop → DUPLICATE.
- GUI order-count confirmation for T1/T2 is trader-touch by definition (Mac side sees journal + dispositions only).

## 8. Exact drop timeline (UTC)

| Time | Action | File | Outcome |
|---|---|---|---|
| 19:56:55 | T3 probe → incoming/ | …T19-57-00.000Z-SIM-B1F-T3-BROKEN-20260714.json (34 B) | rejected/ by 19:57:01 |
| 19:57:16 | T1 att.1 → incoming/ | …T19-57-20.000Z-SIM-B1F-T1-20260714-155720.json (126 B) | rejected/ ≤1 s |
| 20:00:16 | T1 att.2 → root | …T20-03-00.000Z-SIM-B1F-T1-20260714-160300.json (126 B) | rejected/ by 20:00:23 |
| 20:02:01 | MNQ diag → root | …T20-06-00.000Z-SIM-B1F-DIAG-MNQ-20260714-160600.json (133 B) | rejected/ by 20:02:08 |
| 20:03:37 | T1 att.3 (aged 20 s) → root | …T20-10-00.000Z-SIM-B1F-T1-20260714-161000.json (126 B) | rejected/ by 20:05:59 |

MNQ diagnostic payload verbatim:

```
{"symbol":"MNQ","side":"BUY","qty":1,"price":29874.00,"signal_id":"SIM-B1F-DIAG-MNQ-20260714-160600","ts":"2026-07-14T20:06:00.000Z"}
```

---

## Rerun after NQ remount (17:04–17:10 ET / 21:04–21:10Z)

**Context from coordinator:** NT8 Output showed the first-run reject reason verbatim — `symbol NQ does not match strategy instrument MGC`: the strategy had been mounted on an **MGC** chart. §5's share-read hypothesis is REFUTED; the consumer read and parsed every first-run file correctly, and the original T3 PASS was genuine (caveat in §5 withdrawn). Trader remounted PraxisSignalConsumer on NQ 09-26 / Sim101 and re-enabled it (~17:04 ET, fresh "consumer started").

### Rerun result summary

| Test | Result | One-liner |
|---|---|---|
| Baseline (no replay after remount) | **PASS** | 21:04:57Z: 5 old files still in `rejected/`, `processed/` empty, journal still 3-byte BOM, root clean — restart replayed nothing |
| T1 (valid NQ signal → accepted, order submitted) | **PASS** (order count = trader-touch) | ACCEPTED journal line + `processed/` in <1 s |
| T2 (dedupe, same signal_id, new ts) | **FAIL / BLOCKED — consumer died** | Duplicate file NEVER processed (>3.5 min, no FSW pickup, no 15 s rescan pickup); no DUPLICATE line, no move |
| T3 rerun (malformed → rejected) | **FAIL / BLOCKED — consumer died** | Fresh malformed probe also never touched (>80 s) — confirms consumer stopped, not a duplicate-path stall |

### T1 — PASS

Dropped (`.tmp` → `mv` in root) at **21:05:06Z**, filename `2026-07-14T21-05-00.000Z-SIM-B1F-T1-20260714-170500.json` (126 B), contents verbatim:

```
{"symbol":"NQ","side":"BUY","qty":1,"price":29874.00,"signal_id":"SIM-B1F-T1-20260714-170500","ts":"2026-07-14T21:05:00.000Z"}
```

First poll at **21:05:11Z**: file already in `processed/`, journal line written (verbatim, tab-separated, CRLF):

```
SIM-B1F-T1-20260714-170500	2026-07-14T21:05:06.5892981Z	2026-07-14T21-05-00.000Z-SIM-B1F-T1-20260714-170500.json	ACCEPTED
```

`processed/` disposition proves `SubmitBracket` returned true (a submit failure moves the file to `rejected/` after journaling). GUI confirmation that exactly ONE bracket (entry + OCO stop/target) exists on Sim101 is trader-touch. **Timing caveat:** 21:05Z = 17:05 ET, inside the CME 17:00–18:00 ET NQ maintenance halt — the sim engine's async handling of an order submitted into the halt is the prime suspect for what follows.

### T2 — FAIL/BLOCKED: consumer died within 17 s of the T1 order

Dropped at **21:05:23Z**: `2026-07-14T21-07-00.000Z-SIM-B1F-T1-20260714-170500.json` (126 B) — SAME `signal_id`, new ts/filename, contents verbatim:

```
{"symbol":"NQ","side":"BUY","qty":1,"price":29874.00,"signal_id":"SIM-B1F-T1-20260714-170500","ts":"2026-07-14T21:07:00.000Z"}
```

Expected: DUPLICATE journal line + move to `processed/duplicates/` within ~1 s (FSW) or ≤15 s (rescan). Observed: **file still sitting untouched in the root at 21:08:38Z (195 s), and still there at 21:09:03Z**. No journal change, no move, `duplicates/` empty. The dedupe contract itself was therefore NOT exercised — but note no second order can have fired either (no ACCEPTED line, file never processed).

### T3 rerun — FAIL/BLOCKED: confirms consumer stopped

Discriminating probe (zero order risk) dropped at **21:07:39Z**: `2026-07-14T21-08-00.000Z-SIM-B1F-T3-BROKEN-170800.json` (34 B, same truncated JSON as the first run). Observed: **also untouched at 21:09:03Z (84 s)** — where the 15:56Z instance rejected the identical payload in ≤6 s. The watcher/rescan pipeline is dead for ALL files, not just duplicates.

### Analysis

- Consumer was alive at 21:05:06Z (T1 processed in <1 s) and dead by 21:05:23Z (T2 never picked up; nothing processed since, including a malformed probe and ≥12 rescan intervals).
- Leading hypothesis: the T1 market order, submitted at 17:05 ET **during the NQ 17:00–18:00 ET maintenance halt**, was rejected asynchronously by the sim engine, and NT8's default realtime-error handling disabled the strategy (strategy termination stops the watcher, rescan timer, and journal writer). The dedup journal survives (ACCEPTED line present), so a restart will NOT replay T1 — but T2/T3 files are still in the root and will be processed on next start (T2 should then land as DUPLICATE, no order: an accidental T4-flavored check).
- Verbatim disable/error lines are in NT8 Output/Log (VM side, trader-touch). Not observable from the Mac.

### Rerun state at close (21:09Z)

```
root: 2026-07-14T21-07-00.000Z-SIM-B1F-T1-20260714-170500.json   126 B  (T2, unprocessed)
      2026-07-14T21-08-00.000Z-SIM-B1F-T3-BROKEN-170800.json      34 B  (T3 probe, unprocessed)
processed/2026-07-14T21-05-00.000Z-SIM-B1F-T1-20260714-170500.json (T1)
processed/duplicates/ : empty · rejected/ : the 5 first-run files, unchanged
journal: BOM + 1 line (T1 ACCEPTED, quoted above)
```

### What remains

1. Trader-touch: read NT8 Log/Output for the strategy-disable reason (~21:05:1xZ); confirm T1 order outcome in Orders tab (filled/rejected/queued).
2. Re-enable the strategy **outside the 17:00–18:00 ET halt**; its startup catch-up scan will consume the two leftover files — expect T2 file → DUPLICATE (no order; doubles as a journal-reload/no-replay probe) and T3 probe → rejected/.
3. Then T2/T3 formal PASS evidence + T4 restart test.

---
*Mac-side run, headless; no self-certification. First run: T3 PASS, T1 FAIL (root cause MGC mount, since fixed). Rerun: T1 PASS (ACCEPTED + processed/), T2/T3 BLOCKED by consumer death ~17:05 ET. Exactly one ACCEPTED signal all day (journal-proof); zero duplicate orders possible.*
