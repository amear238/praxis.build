# Step 0.6 — n8n Telegram Notification Nodes

**Date:** 2026-05-11
**Session:** sess2
**Beads issue:** Praxis_build-8n6
**Workflow ID:** `EmMbN4sslwIx1ydn`
**Workflow URL:** https://n8n.myzerker626.win/workflow/EmMbN4sslwIx1ydn

## Goal
Add Telegram notifications to the PRAXIS Signal Webhook workflow on three events (success, validation reject, file-write failure) without affecting HTTP response timing.

## Nodes added
Three `n8n-nodes-base.telegram` v1.2 nodes (`resource: message`, `operation: sendMessage`), each with `additionalFields.appendAttribution: false` and `parse_mode: HTML`:

1. **Telegram Notify Success** — text: `✅ PRAXIS signal: {symbol} {side} {qty} @ {price} (id: {signal_id})` (pulls fields from `Validate Signal Payload` node body).
2. **Telegram Notify Rejected** — text: `⚠️ PRAXIS signal REJECTED: {{ $json.error }}`.
3. **Telegram Notify Write Failed** — text: `❌ PRAXIS file write FAILED: {{ ($json.error && $json.error.message) || "file write failed" }}`.

All three hardcode `chatId: 6156528469`.

## Wiring summary
Each Telegram node hangs off the corresponding Respond node, in series (the Respond fires first and returns HTTP, then Telegram runs):

- `Respond 200 OK` → `Telegram Notify Success`
- `Respond 400 Bad Request` → `Telegram Notify Rejected`
- `Respond 500 Write Error` → `Telegram Notify Write Failed`

Because `Respond to Webhook` flushes the HTTP response when the node executes, downstream Telegram calls do not block the response back to TradingView.

Total node count: 10 (was 7).

## Credential reference name
Workflow code references credential by name: **`PRAXIS xarq5bot`** (type `telegramApi`). No bot token is stored in code, files, or this report.

**Note:** On `update_workflow`, n8n auto-assigned an existing credential `Telegram account (networkchuck2)` to all three new nodes (auto-assignment behavior — the named credential `PRAXIS xarq5bot` does not yet exist in n8n). The user must re-point credentials before activation (see next section).

## Validation output
`validate_workflow`: **valid**, nodeCount 10, 3 warnings of type `INVALID_EXPRESSION_PATH` for `$json.error` references in `Respond 500 Write Error`, `Telegram Notify Write Failed`, `Telegram Notify Rejected`. These are runtime-only paths (IF false branch and error output) that the static checker cannot trace — identical pattern exists in the pre-existing `Respond 500 Write Error` node and works at runtime.

`update_workflow`: success, nodeCount 10.

## What user must do next
1. Open n8n → Credentials → New credential → type `Telegram API` → name it exactly **`PRAXIS xarq5bot`** → paste bot token from BotFather (token for the `@xarq5bot` bot).
2. Open the workflow → for each of the 3 Telegram nodes, change the credential dropdown from `Telegram account (networkchuck2)` to `PRAXIS xarq5bot`.
3. Save and re-activate the workflow.
4. Test: POST a valid payload to `/webhook/praxis-signal` and confirm the success message arrives in chat `6156528469`. Then test a rejected payload (missing field) for the warning message.

## Files staged
- `STATUS.md` — Step 0.6 marked done, Current Step → 0.7
- `MANIFEST.md` — added this report entry
- `docs/reports/2026-05-11-sess2-step-0.6-telegram.md` — this file

No code committed (per task constraint).
