# Project: PRAXIS — Automated NQ Futures Trading System
**Last updated:** 2026-07-25 (session 29 — **852 VM-agent delegation workspace LIVE EXECUTOR LEG PROVEN — 852 now closeable; wx7 external build-progress report SHIPPED (497088b). No new repo commit for the 852 proof (artifact lives outside the repo).** The Windows Claude-Code executor ran acceptance assignment `0000-sample-roundtrip` end-to-end: read `AGENT-BRIEF.md`, claimed it, resolved the real Windows path, self-computed the SHA256 of `ASSIGNMENT.md` TWICE (Get-FileHash + certutil), wrote `output/roundtrip-ack.txt` (WINDOWS_PATH / ASSIGNMENT_SHA256 / ACK), set STATUS=DONE — touching NONE of the four §2 hard limits (no NT8, no account, no GUI, no web-fetch). The **orchestrator-auditor independently re-hashed `ASSIGNMENT.md` Mac-side** = `e7744e0b2bbdcfd95057452217f3092253a4b60225c2356b7564bda12267183f` (2331 B, pure LF, zero CRLF) — **EXACT match** to the executor's value; all 4 acceptance criteria PASS (ack complete; WINDOWS_PATH a real `C:\` path not a sandbox path; hash match; STATUS=DONE). No commit token minted — the artifact lives on the Parallels share OUTSIDE the git repo, so there was no staged diff to gate. Orchestrator then grade-noted + archived the assignment (sole folder-mover): `~/praxis-signals/vm-agent/archive/0000-sample-roundtrip-DONE/` with `GRADE.txt`; `assignments/` now empty. Consequence: **852 is fully satisfied — workspace BUILT (session 28) AND delegation channel PROVEN end-to-end — closeable.** The wx7 report (497088b) correctly described the 852 leg as unproven-at-the-time and is a point-in-time artifact — NOT edited. Block 2 backtesting remains the active phase; **4uu VM re-import still pending, unchanged.** DECISION_LOG 2026-07-25 (session 29) row records the proof. Prior session-28 note follows.) (session 28 — **852 VM-agent delegation workspace BRAINSTORMED + DESIGNED + BUILT (Mac-side); 1 commit (design doc).** Brainstorming skill run with Amear → approved design `docs/specs/2026-07-25-vm-agent-delegation-workspace-design.md` (committed `0229da7`, MANIFEST row). 8 locked decisions (§2): D1 queue-that-degenerates (numbered assignments, claim lowest-numbered QUEUED), D2 self-contained folder + in-place STATUS (nothing moves — share-robust, no-memory-safe), D3 praxis-tutor does NOT write here (orchestrator sole write-authority), D4 launchd watcher DEFERRED, D5 4uu stays in b2-data (no re-pathing in-flight), D6 self-containment = stage Windows refs into `reference/` (executor can't reach the Mac repo), **D7 HYBRID executor — anything Windows-end goes to Claude Code IN THE VM *or* Cowork (whichever fits); AGENT-BRIEF generic; NT8-GUI steps flagged [GUI STEP] → Cowork/Amear since Claude Code is terminal-only**, D8 orchestrator-auditor grades every DONE (no self-cert). **BUILT on the share** `~/praxis-signals/vm-agent/` (Windows `C:\Mac\Home\praxis-signals\vm-agent\`, prefix proven `Test-Path b2-data=True`, OUTSIDE the git repo so Claude-Code-in-VM ≠ one-session-per-repo violation): `AGENT-BRIEF.md` (4 hard limits inline + claim loop + STATUS machine QUEUED→CLAIMED→IN_PROGRESS→DONE/BLOCKED + hybrid note), `README.md` (orchestrator post/grade/archive + posting rule), `TEMPLATE-ASSIGNMENT.md`, `reference/{nt8-windows-reference.md,cowork-agent-delegation.md}` staged, `assignments/0000-sample-roundtrip/` posted QUEUED (recompute-hash self-test, no NT8 GUI), `archive/` empty. bd memory `vm-agent-delegation-workspace`. Also: gave Amear verified **Claude Code Windows-11-ARM install** steps (`irm https://claude.ai/install.ps1 | iex`, native ARM64, no WSL) + the standing kickoff line + the `/add-dir` fix for its cwd boundary. **852 NOT closed** — Mac-side acceptance met; the LIVE executor leg (Amear runs Claude Code/Cowork in the VM → round-trips 0000 → auditor grades output/ → PASS) proven next Windows session, NOT faked. **NEXT-CHAT TASK filed = `wx7` (P1):** in a FRESH chat, build a SELF-CONTAINED build-progress report FOR the Claude.ai project-scoping agent — I am an authority ONLY on WHAT I BUILT (not the plan), and EVERY Amear-agreed deviation must be explicitly specified. Orchestrator gate NOT armed; commit plain. Prior session-27 note follows.) (session 27 — **bev teaching-agent IMPLEMENTED + 4uu did NOT advance (Cowork ran a layout-capture pass, not the re-import). 1 file created, no commit yet at time of writing.** (1) **bev IMPLEMENTED:** wrote `.claude/agents/praxis-tutor.md` (name chosen by Amear via AskUserQuestion: `praxis-tutor`, not `corner`) exactly per the approved design `docs/specs/2026-07-24-bev-teaching-agent-design.md`. BEHAVIORS-ONLY — privacy scan (grep adhd/diagnos/medicat/therap/clinical/disorder/symptom/etc.) CLEAN, zero clinical terms in the committed file (repo PUBLIC). `disallowedTools: Bash, NotebookEdit` structurally blocks commit / `bd close` / audit-token mint; keeps Read/Grep/Glob/Write/Edit for state-file reading + teaching-note/Cowork-brief writing. Full teaching contract + grounding (NT8 ref + STATUS/DECISIONS/MANIFEST + Cowork runbook) + hard limits (sim-only, milestones human-gated, DECISIONS append-only) w/ the Block-5 comprehension gate (D-2026-07-04-A) as its teaching target. MANIFEST row added; bev bead noted. **NOT closed — acceptance = a trader dry-run teaching session Amear signs off (can't self-certify).** (2) **4uu NOT advanced:** the new inbound artifact `inbound/nt8-layout-2026-07-24/` (20 screenshots + 00-notes.txt) is a LAYOUT-REFERENCE capture, NOT the re-import — the Cowork agent ran read-only (its constraints log: no NinjaScript import, no F5 compile, Strategy Analyzer Run never clicked, no historical data imported). So NO import/compile/reconcile output exists → no auditor dispatch. v2 brief still live at `~/praxis-signals/b2-data/4uu-VM-brief.md`, needs re-handing to the Cowork agent. Useful crumbs the capture confirms: Import section defaults **Time Zone = UTC** (brief overrides to US Eastern) and **Data Type = Last** (matches required `NQCONT.Last.txt` token); it also partially covers `aac` residuals. Orchestrator gate NOT armed session 27; commit plain. Prior session-26 note follows.) (session 26 — **bev teaching-agent DESIGN APPROVED + 4uu v2 dispatched to the Cowork agent. 0 code changes — docs/ledger only.** (1) **4uu:** trader chose to run the re-import; handed the staged v2 brief (`~/praxis-signals/b2-data/4uu-VM-brief.md`, mirror `outbound/INSTRUCTIONS.md`, clipboarded) to the Cowork agent — **VM work IN PROGRESS at close, no output in `inbound/` yet.** When its import/compile/Analyzer output lands → dispatch read-only orchestrator-auditor to grade §B; on PASS close 4uu → unblocks zi1→ajj→xdr. (2) **bev DESIGN APPROVED (Amear, session 26):** teaching-style package RECEIVED — lives PRIVATELY in Amear's Google Drive (`TEACHING-METHODS-FOR-AMEAR.md`, from a learning-style file + intake questionnaire; clinical). Repo is PUBLIC → **PRIVACY DECISION (AskUserQuestion): commit BEHAVIORS ONLY** (pedagogy rules, zero diagnoses/meds/therapy terms); clinical why stays in Drive. Approved design written to `docs/specs/2026-07-24-bev-teaching-agent-design.md` (via brainstorming skill): `.claude/agents/praxis-tutor.md` (name TBC; Amear may rename `corner`), read-only-ish toolset (no bead-close/commit/token), teaching contract (shape-first, one-concept-per-block, closed menus, externalized numbered steps, combat/building/relay-race metaphors, cross-link, high-challenge+concrete-first-action, quiz-back recall prompts, anti-methods refused, close-on-ONE-handle) + grounding (NT8 ref + state files + Cowork runbook) + hard limits (sim-only, milestones human-gated, Block-5 gate = explain entries/exits/breakers cold+unprompted). **IMPLEMENTATION is next session.** bd memory `bev-teaching-style-package`; MANIFEST row; bev bead notes updated. Orchestrator gate NOT armed; commits plain. Prior session-25 note follows.) (session 25 — **8yb DELIVERED + CLOSED (NT8 Windows reference) + 4uu re-import brief v2 staged.** (1) Built `docs/reference/2026-07-24-nt8-windows-reference.md` — version-stamped **NT8 8.1.7.2**, 7 PRAXIS workflows w/ exact click-paths, merged official Help Guide (research agent) + **20 live VM screenshots + coworker capture notes** (`docs/reference/assets/nt8-2026-07-24/`). Render fault confirmed **CLEARED** on restart (new dialogs paint → bug 518 stays monitor-only). Live corrections: Historical Data window is a vertical **ACCORDION** (Loaded/Download/Import/Export/Get-Market-Replay) not tabs; **Import section has NO instrument field → NT8 derives instrument FROM FILENAME** (= the 4uu blocker, evidence-confirmed); license tooltips advisory-only (Editor/Analyzer open normally); PraxisNoiseAreaBreakout + PraxisSignalConsumer already installed; Sim101 $99,995. bd memory `nt8-windows-reference`; MANIFEST rows; follow-up bead **aac (P3)** = 4 residual captures (RTH template hours, Export/Market-Replay sections, .zip import, Flatten wording). **8yb CLOSED.** (2) **4uu re-import brief REWRITTEN v2** (share `b2-data/4uu-VM-brief.md` + `outbound/INSTRUCTIONS.md`) fixing the 2026-07-24 run's two blockers: filename now **`NQCONT.Last.txt`** (was `NQ 12-25.txt`, missing the `.Last` data-type token) + render-fault restart precondition. Trader chose (AskUserQuestion) a **DEDICATED CUSTOM INSTRUMENT `NQCONT`** (tick 0.25 / pt $20 / CME US Index Futures RTH) so continuous data never collides w/ native NQ and the 09:30 alignment check works; **Time Zone = US Eastern pinned (NOT UTC).** New Step 0b = create NQCONT; Steps 1b/2/3/4 (bar-stamp check, self-computed hash, Analyzer RTH/ExitMode=BandOrVWAP/Contracts=1/≥3mo, §B verify) carried forward. (3) Flushed stranded AUDIT_LOG `f8a` row (own commit). Commits pushed: AUDIT_LOG flush + `docs(8yb)` reference. Orchestrator gate NOT armed; commits plain. Prior session-24 note follows.) (session 24 — Mac-side 4uu prep + delegation-protocol capture (0 repo code changes). **4uu Mac-side READY; VM work IN PROGRESS at close (Cowork agent running).** (1) Removed an NT8 import-format blocker — the continuous CSV is comma/header/7-col which NT8 Historical Data Import rejects → built+validated `b2-data/NQ-continuous-1min.nt8import.txt` (NT8-native `yyyyMMdd HHmmss;O;H;L;C;V`, headerless, 1,857,362 rows, OHLCV byte-identical to source every row, timestamp reformatted only — NO time shift). (2) Reconcile-reference SETTLED (trader, AskUserQuestion): 4uu reconcile = self-contained §B spec-consistency checks; no external TV-alert corpus exists (paper re-implementation) nor is required. (3) 4uu operator brief rewritten FOR THE COWORK AGENT (self-contained, real Windows paths, Analyzer RTH 09:30-16:00 ET / ExitMode=BandOrVWAP / Contracts=1 / ≥3mo, 09:30 bar-stamp definitive check w/ 2026-07-17 sample bars) → share `b2-data/4uu-VM-brief.md` + `outbound/INSTRUCTIONS.md` + clipboard. (4) **Delegation protocol captured:** the 'coworker'/VM operator is Claude in COWORK MODE (Claude desktop, research preview) on Amear's Windows/NT8 box — NOT Claude Code, NOT human → `docs/runbooks/2026-07-23-cowork-agent-delegation-brief.md` + bd memory `cowork-agent-delegation` + MANIFEST. **AUDIT-INTEGRITY FIX:** the executing agent RECOMPUTES hashes/checksums itself before compile/deploy — never accepts a pasted 'already verified' claim (per Cowork's own report the session-23 CRLF hash was pasted to it, incl. as 'auditor re-verified', and NOT self-computed → that 'independent' PASS graded a paste). (5) Filed next-chat beads **8yb** (NT8-Windows research/knowledge-base for teaching) → **bev** (repo teaching-agent adopting Amear's teaching-style package; depends 8yb). Prior session-23 note follows.) (session 23 — orchestrator, NON-BUILD verification session (**0 code commits, 1 audit bead closed**). **4uu file-readiness independently AUDITED + PASSED; CRLF false-blocker resolved.** Coworker relay flagged the VM-deployed `PraxisNoiseAreaBreakout.cs` as a hash MISMATCH (raw sha256 `c22f38fa14…23caa65` / 14,630 B vs repo `35393823e9…2752265` / 14,318 B) and correctly HALTED before F5. Root cause = **LF→CRLF line-ending normalization** when the 719ab11 file landed on Windows 7/17, NOT stale/divergent code: delta 14,630−14,318 = 312 = exactly the file's line count (one +\r per line); the repo file converted LF→CRLF hashes to `c22f38fa14…23caa65` == the VM-reported hash, byte-for-byte. **Independent orchestrator-auditor (bead `Praxis_build-4uu.1`, CLOSED PASS)** re-derived all four criteria from scratch: no-tamper (git diff HEAD clean, traces to 719ab11), LF hash, CRLF-equivalence==VM hash, share-copy parity — verification-only → no commit/token. Closed the flagged "ninjascript/ never synced to the share" gap: placed the verified LF copy + `PROVENANCE.txt` at `~/praxis-signals/b2-data/ninjascript/`. Corrected all-clear brief delivered to coworker (clipboard + share + shown in chat). `bd remember vm-cs-crlf-hash-gate` records the recurring CRLF false-mismatch gotcha. Set up a /loop standing-auditor watch (Monitor `b2tkqy2oj` on `b2-data/inbound`) to auto-grade the next VM stage; ran idle ~hours (VM silent), then stopped the heartbeat at trader request + torn down at close. **Critical path UNCHANGED and still trader-VM-gated: 4uu FILE blocker is DISSOLVED (deployed .cs proven current) — remaining 4uu = F5 compile → import CSV → Strategy Analyzer → reconcile, trader hands.** Prior session-22 note follows.) (session 22 — orchestrator, **2 audited commits: 1u6 tz-conversion SHIPPED (attempt-2 lossless) + AUDIT_LOG flush.** **1u6 CLOSED** `f23b648`: lossless UTC→ET conversion of the continuous NQ series — route front/back seam handoff on source-tz (UTC) date, relabel to America/New_York only after routing+dedup → partition bit-identical to baseline. Attempt-1's ~2,498-min seam loss ELIMINATED; auditor's own re-run proved distinct_instants(ET)==baseline==**1,857,362**, seam-gap==0, §4 overall=PASS, 09:30-ET anchor verified; new self-test Case E discriminates fix(9) from naive(6); §4.2 break-check regraded to true intent (48 real illiquid halt-prints retained as INFO, auditor-confirmed legit); protected areas byte-identical; token ac89c0f5. **f8a CLOSED** `9608843`: AUDIT_LOG flush, 3 stranded rows (8zd/pb9/1u6) committed append-only, flush-mode PASS; one rolling row stranded by design. **ET continuous series now on disk + ready for NT8 re-import** (`~/praxis-signals/b2-data/NQ-continuous-1min.csv`, 1,857,362 bars, ET wall-clock); trader re-import brief prepared+clipboarded. **Critical path is now 100% trader-VM-gated: 4uu (re-import + compile + Strategy-Analyzer reconcile) → zi1 → ajj → xdr; no autonomous Mac-side work remains on it.** Prior session-21 note follows.) (session 21 — orchestrator, **2 audited commits + 1u6 FAILED-audit/parked + quota interruption.** **44/44 raw NQ exports LANDED** (coworker completed the pull: 22 daily + 22 minute in `~/praxis-signals/b2-data/raw/`). **8zd CLOSED** `c571c74`: `b2_stitch.py` now parses the REAL NT8 format (semicolon-delimited, headerless — daily `YYYYMMDD;O;H;L;C;V` + minute combined `YYYYMMDD HHMMSS`); full-44 volume-only stitch emits the continuous series (1,857,362 bars, 21 seams, 5 no-overlap boundary-fallback seams flagged) + §4 report; caught+fixed a latent OI-alias substring bug; self-test 4/4; auditor PASS token 3e82fca6. **pb9 CLOSED** `03281e6`: b2data-watch OI-absent demoted STOP→informational (D-2026-07-21-A), completeness+sanity gates intact, self-test green. **⚠️ 1u6 (P2, PARKED):** stitch surfaced that the series is timestamped **UTC not ET** (confirmed via DST-crossing test) — must convert to America/New_York for the 09:30-ET-anchored strategy. Attempt-1's conversion introduced **silent seam data loss** (~2,498 pre-roll evening ETH minutes dropped at all 16 roll-eve dates, 1,857,362→1,855,411, false overall=PASS) — **auditor CAUGHT it, FAILed, NOT committed, reverted to committed-good**; full fix spec in bead 1u6 notes. Fix subagent then hit the **account session-limit** (resets ~18:20 America/Toronto) mid-retry → quota-interruption wrap, did NOT retry the quota-dead run. AUDIT_LOG.md stranded rows left for the flush protocol. **Next session: re-dispatch 1u6 attempt-2 first (auditor must re-verify instant-count invariant + seam-gap==0), then 4uu.** Prior session-20 note follows.) (session 20 — **🟢 OI_BLANK STOP RESOLVED (6hu CLOSED).** Trader authorized the **volume-only crossover** roll trigger via AskUserQuestion; **D-2026-07-21-A** appended (append-only) resolving the D-2026-07-17-A OPEN RISK. Only the roll TRIGGER changes (drop OI from the crossover test); **Difference/Panama back-adjustment UNCHANGED**, pull range + spec-§4 validation unchanged. No code change needed — `scripts/b2_stitch.py` already ships the `--allow-volume-only` path whose contract required exactly this DECISIONS append before use; the tool still prints a loud VOLUME-ONLY DEVIATION banner per run (logged, not silent). **6bw real-data run now unblocked in volume-only mode once raw/ fills** (chain: 6bw→4uu→zi1→ajj→xdr). Prior session-19 note follows.) (session 19 — orchestrator, 2 audited commits + OI_BLANK stop. **6bw CLOSED** c2f674d: Python Vol/OI-crossover+Difference stitch tool pre-built ahead of data (scripts/b2_stitch.py + config/b2-stitch-schema.json + self-test; schema-configurable CSV parse, deterministic vol/OI-crossover roll from daily series, additive Panama back-adjustment, spec-§4 validation report, HARD OI-blank refusal — no silent volume-only fallback; stdlib-only; fixture self-test 4/4; **VALIDATION-PENDING until raw/ lands**). **AUDIT_LOG flush CLOSED (8q5)** 39d2301: 24 stranded rows flushed append-only in flush-mode (auditor PASS c0a1050c); the session-12 `block1-milestone-report` orphan (2f8cd726) VOID-annotated per trader decision (AskUserQuestion) — it LANDED as d405125, a slug/message wording mismatch NOT an invalid mint (DECISION_LOG 2026-07-20T17:02Z); follow-up **9qx** filed for a proper RECONCILED annotate verb (build only if it recurs). **🛑 OI_BLANK STOP (6hu, P1, OPEN — trader decision):** coworker's export began mid-session; daily files are 6-col `date;O;H;L;C;volume` with **NO open-interest column** — confirmed a HARD NT8 platform limit (OI structurally absent from futures export schema at every interval), so re-export-WITH-OI is impossible via this toolchain. b2data-watch daemon fired OI_BLANK on all landed files (reports/landing-log.txt). Trader-LOCKED Vol/OI-crossover roll is blocked → **AWAITING TRADER SIGN-OFF**: volume-only crossover fallback + DECISIONS append resolving D-2026-07-17-A OPEN RISK, vs source daily OI elsewhere. Coworker reports the trader verbally approved volume-only in-VM and proceeded, but **NO DECISIONS append exists in-ledger yet — not formally authorized here.** Export progress at close: 6/22 daily (JUN21,SEP21,DEC21,MAR22,JUN22,SEP22), 0/22 1-min (17 contracts still need chunked NT8 downloads first). **cn4 (Telegram token rotation) untouched — trader-gated.** Prior session-18 note follows.) (session 18 — orchestrator, 1 audited commit + daemon activation. **b2-data-mailbox 3q4 CLOSED** 1dfe7a8: bidirectional VM↔Mac shared-folder mailbox (`~/praxis-signals/b2-data/` — outbound/INSTRUCTIONS.md Mac→VM, inbound/status.txt VM→Mac, raw/, reports/) + launchd watcher **build.praxis.b2data-watch** (clone of signals-sweep-daemon; internal-disk TCC-safe install; seen-state idempotency; file-settle guard) that auto-notifies on each landing, relays status.txt edits, and **auto-runs RAW-LANDING validation** (OI gate + 44-file completeness + per-file sanity + coarse coverage) → reports/raw-landing-validation.md with a machine-summary line. **Daemon INSTALLED + LIVE** (pid confirmed, launchctl loaded); mailbox scaffolded; **outbound/INSTRUCTIONS.md seeded with the export brief → coworker reads it directly in the VM, so the trader is no longer the VM↔Mac relay.** Auditor PASS (token 95a6bb0e), self-test 5/5 scenarios pass. Telegram pings still DRY-RUN only (notifications land in reports/ + landing-log.txt in the share regardless) — filed **cn4** (token rotation). **lhw CLOSED** — trader logged into NT8 + flattened Sim101. Follow-ups filed for the fresh chat: **6bw** (Python Vol/OI-crossover+Difference stitch — pre-build ahead of data, left READY; only its validation waits on raw/), **cn4** (Telegram activation). **AUDIT_LOG.md ~22 stranded PASS rows → flush next session** (runbook docs/runbooks/2026-07-10-audit-log-flush.md). Scope wall held: raw-landing validation ONLY, NOT b2-data spec §4 continuous validation (that's 6bw). Prior session-17 note follows.) (session 17 — coordination/coworker-relay session, NON-BUILD. (1) Stakeholder **build-review presentation** authored + committed f3c615f (docs/presentations/build-review.html — plain-language slide deck; published claude.ai Artifact; bd memory key build-review-presentation records the update-in-place recipe). (2) Coworker repo access resolved: repo amear238/praxis.build is PUBLIC → clone/browse, no grant needed (reaffirms D-2026-07-08-A). (3) **D-2026-07-17-A LOCKED (trader):** the locked Vol/OI-crossover+Difference roll convention is NOT natively buildable in this NT8 install → continuous NQ series will be built OUTSIDE NT8 in Python (Mac-side, per Q8); locked convention itself UNCHANGED. Coworker exports individual quarterly contracts (1-min OHLCV + daily bars w/ volume+OI) to \\Mac\praxis-signals\b2-data\raw\; Claude stitches+back-adjusts+validates Mac-side, re-imports to NT8. **OPEN RISK:** if free provider lacks open interest → volume-only crossover fallback needs trader sign-off (append DECISIONS) BEFORE use. (4) **hlw data-pull IN PROGRESS:** coworker cracked the NT8 downloader trick (root+MMMYY+date-range auto-pulls every quarterly spanning the range); 10 contracts DOWNLOADED into NT8's history DB — but only 5 are in-window (06-21, 09-21, 03-26, 06-26, 09-26); the 2020 contracts are extra/not needed; the **17 middle contracts 12-21→12-25 still to pull** (chunked, ≤~18mo/request); **NONE exported to raw/ yet (download≠export).** (5) NT8 crash incident: a full-2021-2026-span single request spawned 1,800+ error dialogs, pegged CPU, froze UI → force-restart; **no data lost**; filed **Praxis_build-2vu P3** (chunking workaround). NT8 now sits at LOGIN screen — trader password required. (6) OI good news: daily bars cached locally to 2017 → OI export is quick, still needs first-export column verification. Git at close: f3c615f pushed; STATUS/DECISIONS committed this wrap; pre-existing AUDIT_LOG.md stranded rows left untouched. Prior session-16 note follows.) (session 16 — orchestrator dispatch, 3 audited commits: **b2-ruleset-source CLOSED** 2a3f192 (canonical Zarattini-Aziz-Barbon Noise-Area ruleset fetched from SSRN 4824172 / full 43-pg St. Gallen open-access copy → docs/specs/2026-07-16-b2-noise-area-ruleset.md; scout-flagged "gaps" were PRAXIS-vs-paper divergences, auditor-confirmed no fabricated cites) + **b2-signal-repro half-open** 719ab11 (paper-faithful ninjascript/PraxisNoiseAreaBreakout.cs authored — 09:30-anchored gap-adjusted noise band, HH:00/HH:30 checkpoints, reverse/re-enter, 16:00 flat, ExitMode sole tunable, params fixed const, no look-ahead; audit caught+fixed 2 smuggled optimizable knobs; VM compile+Strategy-Analyzer reconcile remains) + **6h7 CLOSED** de46c56 (signal-template.json regenerated to live consumer contract signal_id/symbol/side/qty/price/ts +opt stop/target; version-controlled canonical signals/signal-template.json + README, .gitignore negation; ~/praxis-signals copy refreshed). **Trader decisions (DECISION_LOG 2026-07-16, AskUserQuestion): strategy LOCKED paper-faithful — 16:00 ET flat (NOT 11:30), reverse&re-enter (NOT one-position), replicate-first with FIXED params (supersedes D-2026-07-15-B Q7 optimize-set — noise-mult & stop-ATR have no paper analog, WFA optimization deferred to a later arm).** Prior session-15 note follows.) (session 15 — orchestrator dispatch, 2 audited commits: **b2-spec-gaps CLOSED** 89f04b9 (Block-2 gaps Q2-Q9 trader-resolved via AskUserQuestion → DECISIONS.md D-2026-07-15-B) + **b2-data Mac-side** 26ce939 (NQ data-acquisition spec + config/backtest-cost-model.json encoding Q5 + VM operator runbook; **roll convention LOCKED** = volume/OI-crossover + back-adjusted Difference; hlw half-open — VM NT8 physical minute pull + validation remains). Prior session-14 note follows.) (session 14 — orchestrator dispatch, 3 audited commits: **Q1 ANSWERED** 8e739e4 (Noise-Area Breakout is NOT TradingView-exportable — no Pine source in-repo → re-implement entry logic in NinjaScript, backtest in NT8 Strategy Analyzer; trader CONFIRMED the direction, unlocks Block-2; report docs/reports/2026-07-15-b2-q1-strategy-exportability.md). **btb uniform-reject fix** 9d25243 (trader DECLINED the REJECTED-GEOMETRY journaling convention → geometry rejects now handled like parse/schema rejects: Print+Log+move to rejected/, NO journal line; rejected/ file is the at-rest trace; btb still BLOCKED on the attended VM sim test). **10i BUILT** 9905338 (Telegram→Claude read/report/status-ONLY control channel — fixed-argv allowlist, no claude -p passthrough, sender-restricted to trader chat id twice; STAGED not activated; registered Stop hook left untouched; blocked on trader activation). Block-2 execution skeleton created + dep-wired (b2-spec-gaps → data/signal-repro → wfa → mc → refdist). Prior session-13 note follows.) (session 13 — Block 2 opened with 4 audited commits: **btb code LANDED** 59268a9 (OCO-reuse root-caused as NT8 managed-bracket cascade artifact — no OCO string in our code; fail-closed market-geometry gate added; bad-geometry signals → rejected/, zero orders — [session-14 note: the REJECTED-GEOMETRY journaling shown here was DECLINED by the trader and removed at 9d25243]). **s1c Block-2 scope proposal** 113ef5b (docs/specs/2026-07-15-block2-backtesting-scope-proposal.md — 8 SHM PROPOSALS, CANDIDATE exit checklist, 9 spec-gap Qs; Q1 = TV strategy export vs re-implement is the Block-2 hard blocker). **qxd CLOSED** 31eb6cc (n8n false-failure: Validate emitted error:'' judged failed by n8n-core retry loop; fixed in prod workflow, webhook 200 now 0.043s/1 attempt/1 file; NEW trader-touch bead lhw — SIM-QXD-TEST-0001..0004 reached the running consumer, check Sim101). **01h CLOSED** 532f6da (harness no-fire = nothing was watching; RETIRED per DECISION_LOG 19:20Z) + **0s7 cleanup CLOSED** 0086e5a (VM report captured, share archived, live path verified). Prior session-12 note follows.) (session 12 — B1-f COMPLETE: trader confirmed the 11:12:45 ET standing bracket in the NT8 GUI (entry filled 29820, stop 29741.25 + target 29901.25 both working, OCO intact, no Log errors — transcribed evidence in docs/reports/2026-07-14-b1f-t1-t3-mac-run.md). T1-T4 all PASS; ct5 + 9tl closed. **BLOCK 1 MILESTONE TRADER-SIGNED-OFF 2026-07-15 (Amear)** on the independent evidence audit d405125; §6.2 OnTermination-journaling decline RATIFIED (D-2026-07-15-A); btb chosen post-milestone, next-session first dispatch. Current block is now Block 2 — Backtesting. ⚠️ Sim101 may still hold the long 1 NQ + working bracket; trader's call. Prior session-9 note follows.) (session 9 cont. — VM run hit an ENVIRONMENT blocker, root-caused + cleared: NT8 WPF DirectWrite glyph-metrics exception storm (bead 518, P2) prevented ANY new window rendering (human or automation) and burned 3.98 GB trace @231 MB/min; earlier "input injection broken" diagnosis was WRONG (clicks proven landing correctly). Trader NT8 restart cleared it — fresh pid 9672 storm-free 0 bytes/5 min, trace bloat cleaned. T1-T4 NOT RUN yet (honest NOT-RUN, not FAIL — strategy never reached Realtime); .cs installed hash-verified. Automated hybrid harness ARMED in VM: journal-file go-signal -> T1-T3 unattended -> NEEDS-TRADER-T4-RESTART.txt handshake -> report. Awaiting trader's 4 GUI steps. Blocked report collected: docs/reports/2026-07-12-b1f-t1-t4-vm-run-blocked.md)

## Current Phase
Block 2 of 6 — Backtesting (Block 1 milestone trader-signed-off 2026-07-15 — DECISIONS.md "Block 1 milestone" entry; evidence audit d405125). **Q1 RESOLVED + trader-confirmed 2026-07-15 (session 14):** strategy = Zarattini-Aziz-Barbon NQ Noise-Area clock-anchored breakout, re-implemented in NinjaScript, backtested in the NT8 Strategy Analyzer (path b — NOT TV-exportable). Toolchain: NT8 Strategy Analyzer (WFA 252/63/63, 1-tick, $2.96 RT) + FirstRate 1-min NQ history + forward Rithmic tick for OOS; Python = downstream Monte-Carlo layer over the exported OOS trade list. Remaining spec gaps Q2-Q9 tracked in bead b2-spec-gaps.

## Current Step
**Block 1 delivery pipe COMPLETE on sim data:** webhook -> local n8n -> atomic outbox write -> persistent KeepAlive 1s-sweep daemon (bursts <5s, was WatchPaths; D-2026-07-12-A) -> ~/praxis-signals -> Parallels VM share. Latency + idempotency verified (B1-d), offline drill passed (B1-e), silent-loss gap closed by stuck-backlog detector (F-B1e-1, 3 launchd agents healthy). **B1-f NinjaScript FileSystemWatcher consumer SOURCE BUILT (ct5, ed2bc9e):** SIM-only account guard, in-file signal_id dedup journal (9tl contract, at-most-once), bracket orders; static audit PASS. **B1-f COMPLETE 2026-07-15:** T1-T4 all PASS (Mac-side runs sessions 10-11 after VM harness no-fire, bead 01h; T2 dedupe DUPLICATE-journaled, T3 malformed rejected, T4 no-replay across the 9:43 restart) and the corrected-geometry T1 bracket trader-confirmed standing in the NT8 GUI (fill 29820, stop 29741.25 + target 29901.25 working, no Log errors). ct5 + 9tl CLOSED. **Block-1 milestone TRADER-SIGNED-OFF 2026-07-15 (Amear)** on evidence audit d405125; §6.2 OnTermination-journaling decline ratified (D-2026-07-15-A). Current step is now **Block 2 planning** (first dispatch next session is btb, trader-chosen). Known-open bugs carried: btb (P2, OCO-reuse cascade + geometry pre-validation), 518 (P3 monitor-only), 01h (P3), qxd (P3).
**2026-07-10 concurrency incident CLOSED:** a commit race between two concurrent sessions briefly misattributed the /progress plugin diff to bead 22r (7f54ba9). Reconciled by single session per HANDOFF plan: e76f5c6 (jpe plugin) + 1c53a18 (22r detector), both freshly audited. Session 6: trader reviewed + ACCEPTED the incident; **v6y CLOSED at fec1722** — audit token now binds the staged tree hash and is re-verified at commit time, tokens strictly single-use, and commits are DENIED while another claude session has cwd in this repo (one-session-per-repo enforced by hook + runbook, no longer just policy).

## Blockers
- **🟢 OI_BLANK — RESOLVED (6hu CLOSED, session 20):** trader authorized the **volume-only crossover** roll trigger (AskUserQuestion); **D-2026-07-21-A** appended resolving D-2026-07-17-A OPEN RISK. NT8 cannot export OI (hard platform limit) so re-export-with-OI was impossible; volume-only crossover keeps Difference back-adjustment unchanged (seams may shift ~1–2 days, point size preserved). `scripts/b2_stitch.py --allow-volume-only` is now authorized (its guardrail required this exact DECISIONS append). No longer a blocker; 6bw real-data run proceeds in volume-only mode once raw/ fills.
- **🟢 hlw data-pull — COMPLETE (44/44 landed session 21) + stitched to lossless ET series (1u6, session 22):** all 22 daily + 22 minute files in `~/praxis-signals/b2-data/raw/`; continuous back-adjusted ET series built at `~/praxis-signals/b2-data/NQ-continuous-1min.csv` (1,857,362 bars, §4 PASS). No longer a blocker. hlw bead likely closeable (trader/VM sign-off call — see Next Action).
- **🔴 4uu NT8 re-import (Block-2 critical path) — TRADER/VM-gated:** the ET continuous series exists on disk but must be re-imported to NT8, then PraxisNoiseAreaBreakout.cs compiled + Strategy-Analyzer reconciled. This is the ONLY thing between here and the WFA/MC/refdist chain. Trader brief clipboarded (session 22). **[session 23] FILE sub-blocker DISSOLVED — file-readiness independently audited PASS (bead 4uu.1): the VM-deployed .cs is proven byte-identical to committed 719ab11 in Windows CRLF form (the hash "mismatch" was LF→CRLF, not stale/divergent); safe to F5 as-is. Verified LF copy + PROVENANCE.txt now on the share at b2-data/ninjascript/. Remaining 4uu = compile + import + Strategy-Analyzer reconcile, trader hands.** **[session 24] Mac-side prep DONE + VM IN PROGRESS (Cowork agent): NT8-import file `NQ-continuous-1min.nt8import.txt` built+validated (comma CSV won't import; native semicolon file will), reconcile-reference SETTLED = spec-consistency (§B), operator brief rewritten for the Cowork agent (`b2-data/4uu-VM-brief.md`). Awaiting compile log / Analyzer output → auditor grades that stage.** **[session 25] The 2026-07-24 import run FAILED on two now-fixed causes (coworker report `inbound/4uu-VM-status-2026-07-24.md`): filename `NQ 12-25.txt` was missing the `.Last` token, and a `UCEERR_RENDERTHREADFAILURE` render fault silently dropped clicks. Render fault since CONFIRMED cleared (8yb capture). Brief REWRITTEN v2 → dedicated custom instrument `NQCONT` (trader choice), correct filename `NQCONT.Last.txt`, Time Zone=US Eastern, restart precondition. NEXT: hand v2 brief to the Cowork agent → import→F5→Analyzer→§B → output to `inbound/` → auditor grades → on PASS close 4uu.**
- B1-c-fu (P2): stale-heartbeat Telegram alert verified dry-run only — live-fire pending (ties to open Telegram token rotation). Parallels prlctl share-config is Pro/Business-only; this Mac runs Standard, so VM-share changes are GUI-only (documented).

## Next Action When Resuming
**SESSION-29 RESUME (fresh chat — 852 live executor leg PROVEN + closeable; wx7 report shipped 497088b; bev still awaiting dry-run; 4uu still awaiting VM output):**
- **🟢 852 (P2) — PROVEN end-to-end, CLOSEABLE.** The `0000-sample-roundtrip` acceptance assignment round-tripped through the Windows Claude-Code executor and PASSED an independent orchestrator-auditor re-hash (Mac-side SHA256 `e7744e0b…67183f` == executor's value, all 4 criteria PASS); assignment archived to `~/praxis-signals/vm-agent/archive/0000-sample-roundtrip-DONE/` with `GRADE.txt`, `assignments/` empty. Workspace BUILT (session 28) AND channel PROVEN → **close 852** (no commit token — the proof artifact lives outside the repo). Going forward, new 4uu-style VM handoffs route through the `vm-agent/` workspace. Design: `docs/specs/2026-07-25-vm-agent-delegation-workspace-design.md`; bd memory `vm-agent-delegation-workspace`.
- **✅ wx7 (P1) — DONE, SHIPPED 497088b** (`docs(wx7): self-contained build-progress report for the Claude.ai scoping agent`). Point-in-time artifact — do NOT edit it (it correctly described the 852 leg as unproven at write time).
- **🟢 bev (P2) — BUILT, awaiting Amear's dry-run sign-off to close** (unchanged). Invoke `praxis-tutor`, have it teach one PRAXIS/NT8 task end-to-end; if it teaches the way you want → close bev; else edit `.claude/agents/praxis-tutor.md`.
- **🔴 4uu (P1, TRADER/VM) — still awaiting real VM output in `inbound/`** (unchanged). On boot `ls ~/praxis-signals/b2-data/inbound/`; v2 brief live at `~/praxis-signals/b2-data/4uu-VM-brief.md`. New 4uu-style handoffs should route through the `vm-agent/` workspace (852); 4uu itself stays in b2-data until it closes.
- **aac (P3), cn4 (P2, trader-gated):** unchanged. **Hygiene:** AUDIT_LOG.md's one rolling row is by-design. Orchestrator gate NOT armed session 29; commits plain.

**SESSION-28 RESUME (fresh chat — NEW TOP TASK wx7 external progress report; 852 built awaiting live VM round-trip; bev awaiting dry-run; 4uu still awaiting VM output):**
- **🔴 wx7 (P1) — NEW TOP TASK, Amear-directed: BUILD THE SELF-CONTAINED BUILD-PROGRESS REPORT for the Claude.ai project-scoping agent.** In a FRESH chat, produce a standalone document the original scoping agent (Claude.ai — it has NO access to this repo/beads) can read cold to see how the build is progressing. **HARD CONSTRAINTS (Amear, verbatim intent):** (1) I am an authority ONLY on **what I built** — not on the plan/strategy (that is the scoping agent's domain; do not re-own or re-argue the plan). (2) Report factually what was built/shipped + current state (ground in STATUS.md/DECISIONS.md/MANIFEST/beads). (3) **Every DEVIATION from the original plan that Amear AGREED to must be EXPLICITLY specified** (each deviation + note that Amear approved it). Self-contained = carries its own context. Candidate deliverable: `docs/reports/` doc + optional shareable artifact. Full brief in bead `wx7`.
- **🟢 852 (P2) — BUILT Mac-side, NOT closed. ACTION (Amear/Windows): run the LIVE round-trip.** Launch Claude Code in the VM: `cd C:\Mac\Home\praxis-signals\vm-agent` → `claude` → paste the kickoff line (read `AGENT-BRIEF.md`; work lowest-numbered QUEUED = `0000-sample-roundtrip`; recompute the SHA256 of its `ASSIGNMENT.md` yourself; write `output/roundtrip-ack.txt`; set STATUS=DONE). Then on the Mac: dispatch the read-only orchestrator-auditor vs the sample's embedded acceptance criteria (real `C:\` path, hash matches auditor's own, ack present) → on PASS **close 852**. If Claude Code can't leave its home cwd: `/add-dir C:\Mac\Home\praxis-signals` or relaunch from that dir. Design: `docs/specs/2026-07-25-vm-agent-delegation-workspace-design.md`; bd memory `vm-agent-delegation-workspace`.
- **🟢 bev (P2) — BUILT, awaiting Amear's dry-run sign-off to close** (unchanged from session 27). Invoke `praxis-tutor`, have it teach one PRAXIS/NT8 task end-to-end; if it teaches the way you want → close bev; else edit `.claude/agents/praxis-tutor.md`.
- **🔴 4uu (P1, TRADER/VM) — still awaiting real VM output in `inbound/`** (unchanged). On boot `ls ~/praxis-signals/b2-data/inbound/`; still only `4uu-VM-status-2026-07-24.md` + `nt8-layout-2026-07-24/` (both layout-capture, not the re-import). v2 brief live at `~/praxis-signals/b2-data/4uu-VM-brief.md`. Going forward, NEW 4uu-style handoffs should route through the `vm-agent/` workspace (852); 4uu itself stays in b2-data until it closes.
- **aac (P3), cn4 (P2, trader-gated):** unchanged. **Hygiene:** AUDIT_LOG.md's one rolling row is by-design. Orchestrator gate NOT armed session 28; commits plain.

**SESSION-27 RESUME (fresh chat — NEW TOP TASK 852; bev IMPLEMENTED awaiting dry-run; 4uu re-handed to Cowork, awaiting output):**
- **🟢 852 (P2) — NEW, Amear's explicit next-session task: BUILD THE VM-AGENT DELEGATION WORKSPACE.** Amear wants a dedicated directory for the Windows VM (Cowork) agent holding ALL delegated VM tasks, with an **ASSIGNMENT AREA** where the orchestrator posts a task and the Cowork agent picks it up and executes. Generalizes the current b2-data-only mailbox (`~/praxis-signals/b2-data/{outbound,inbound}`) into a reusable, task-agnostic delegation area. **Use the brainstorming skill FIRST (new structure).** Candidate: `~/praxis-signals/vm-agent/` on the Parallels share (surfaces at `C:\Mac\Home\...`), with an `assignments/` area + lifecycle (assigned→in-progress→completed/output), a root AGENT-BRIEF encoding standing rules (sim-only, recompute-hashes, real Windows paths, STOP-on-live) tied to the Cowork runbook, and a decision on whether praxis-tutor writes its Cowork briefs there. Full design in the bead. ACCEPTANCE: workspace created + documented assignment contract + a sample assignment round-trips; MANIFEST/STATUS updated.
- **🟢 bev (P2) — BUILT, awaiting your dry-run sign-off to close.** `.claude/agents/praxis-tutor.md` exists, behaviors-only, privacy-clean. **ACTION (Amear): run a live dry-run** — invoke the `praxis-tutor` agent and have it teach you one PRAXIS/NT8 task end-to-end (~20 min); it should go shape→link→rep→externalize→close-on-one-handle, quiz you back cold, and refuse to advance on a nod. If it teaches the way you want → **close bev**. If it needs tuning, edit `.claude/agents/praxis-tutor.md` (design source: `docs/specs/2026-07-24-bev-teaching-agent-design.md`) and re-run. NOTE: bev's work this session is UNCOMMITTED at handoff unless a commit landed — check `git status`.
- **🔴 4uu (P1, TRADER/VM) — RE-HANDED to the Cowork agent at session-27 close (brief clipboarded + `outbound/INSTRUCTIONS.md` current); AWAITING VM output in `inbound/`.** It did NOT run the re-import the prior time (layout-capture pass instead). On boot: `ls ~/praxis-signals/b2-data/inbound/` — if real import/compile/Analyzer output landed (past `4uu-VM-status-2026-07-24.md` and the `nt8-layout-2026-07-24/` capture) → dispatch the read-only orchestrator-auditor vs §B; on PASS close 4uu. The v2 brief remains live at `~/praxis-signals/b2-data/4uu-VM-brief.md` (mirror `outbound/INSTRUCTIONS.md`, clipboardable) if it needs re-handing again — its steps: create custom instrument `NQCONT` → copy import file to `NQCONT.Last.txt` → Import with **Time Zone = US Eastern** (capture confirms it defaults to UTC — must override) → STEP 1b 09:30 bar-stamp check → self-compute .cs SHA256 → F5 → Strategy Analyzer (RTH 09:30–16:00 ET, ExitMode=BandOrVWAP, Contracts=1, ≥3mo) → §B verify → drop to `inbound/`. When real import/compile/Analyzer output lands → dispatch the read-only orchestrator-auditor vs §B; on PASS **close 4uu → unblocks zi1→ajj→xdr.** ±1-bar shift at STEP 1b → re-stamp Mac-side from `NQ-continuous-1min.csv`, do NOT fudge timestamps in NT8.
- **aac (P3):** partially fed by the `nt8-layout-2026-07-24/` capture (broad layout re-shot) but its named residuals — RTH template hours (Tools>Trading Hours), Export/Get-Market-Replay accordion sections, .zip import, exact Flatten wording — remain uncaptured. Optional Cowork pass.
- **cn4 (P2, trader-gated):** Telegram token rotation — untouched.
- **Hygiene:** AUDIT_LOG.md's one rolling row is by-design. Orchestrator gate NOT armed session 27; commits plain.

**SESSION-26 RESUME (fresh chat — 4uu v2 dispatched to Cowork agent (awaiting VM output); bev design approved, ready to implement):**
- **🔴 FIRST ACTION — check for 4uu output.** The v2 brief was handed to the Cowork agent this session; VM work was IN PROGRESS at close. On boot: `ls ~/praxis-signals/b2-data/inbound/` — if new import/compile/Strategy-Analyzer output has landed (past the old `4uu-VM-status-2026-07-24.md`), **dispatch the read-only orchestrator-auditor** to grade it against §B (checklist `b2-data/4uu-reconcile-checklist.md` = repo `docs/reports/2026-07-16-4uu-noise-area-strategy.md`). On PASS **close 4uu → unblocks zi1 (WFA) → ajj (MC) → xdr (refdist).** Two failure modes the Cowork agent was told to flag: (a) render fault still in the Log → NT8 restart; (b) STEP 1b bar-stamp shows a ±1-bar shift → it STOPS and I re-emit Mac-side from `NQ-continuous-1min.csv` (regen `.nt8import.txt` via scratchpad `nt8_import_format.py`) — do NOT fudge timestamps in NT8. If no output yet, the brief is still live at `~/praxis-signals/b2-data/4uu-VM-brief.md` (+ clipboard) — re-hand it.
- **🟢 bev (P2) — DESIGN APPROVED, IMPLEMENT NOW.** Full approved spec: `docs/specs/2026-07-24-bev-teaching-agent-design.md`. Build `.claude/agents/praxis-tutor.md` (ask Amear to confirm name — he may want `corner`) exactly per the spec: BEHAVIORS-ONLY (no clinical terms — repo is PUBLIC), read-only-ish toolset (no bead-close/commit/token), teaching contract + grounding (NT8 ref + STATUS/DECISIONS/MANIFEST/beads + Cowork runbook) + hard limits (Block-5 gate = quiz entries/exits/breakers cold+unprompted). Teaching-style source is Drive-only (bd memory `bev-teaching-style-package`) — do NOT re-hunt the repo for it. Verify = a trader dry-run teaching session Amear approves; then commit + register + close bev.
- **aac (P3):** residual NT8-reference captures (RTH template hours, Export/Get-Market-Replay, .zip import, Flatten wording) — optional Cowork pass, low priority.
- **cn4 (P2, trader-gated):** Telegram token rotation — untouched.
- **Hygiene:** AUDIT_LOG.md's one rolling row is by-design. Orchestrator gate NOT armed session 26; commits plain. Git + dolt pushed at close.

**SESSION-25 RESUME (superseded by session 26 — 8yb shipped; 4uu v2 now dispatched, bev design approved):**
**SESSION-25 RESUME (fresh chat — 8yb shipped; 4uu re-import v2 brief staged and dispatchable):**
- **🔴 FIRST ACTION — dispatch 4uu re-import v2 to the Cowork agent.** The brief is ready at share `~/praxis-signals/b2-data/4uu-VM-brief.md` (mirror `outbound/INSTRUCTIONS.md`) — hand it to the Cowork agent (clipboard/share). It fixes both 2026-07-24 failure causes: create dedicated custom instrument **`NQCONT`** (tick 0.25 / pt $20 / CME RTH) → copy the import file to **`NQCONT.Last.txt`** (the `.Last` token was the missing piece) → Import with **Time Zone = US Eastern** → STEP 1b 09:30 bar-stamp alignment vs native `NQ 12-25` (2026-07-17: our 09:30:00 Open=28608.75 must be the first RTH bar) → self-compute the .cs SHA256 (CRLF `c22f38fa…`, LF-equiv `35393823…`) → F5 → Strategy Analyzer (RTH 09:30–16:00 ET, ExitMode=BandOrVWAP, Contracts=1, ≥3mo) → §B spec-consistency verify → drop output to `inbound/`. When it lands: dispatch the **read-only orchestrator-auditor** to grade against §B; on PASS **close 4uu → unblocks zi1 (WFA) → ajj (MC) → xdr (refdist)**. If STEP 1b shows a ±1-bar shift → re-stamp Mac-side (source `NQ-continuous-1min.csv`; regen `.nt8import.txt` via scratchpad `nt8_import_format.py`), do NOT fudge timestamps in NT8.
- **✅ 8yb DONE + CLOSED:** `docs/reference/2026-07-24-nt8-windows-reference.md` (NT8 8.1.7.2, version-stamped, 7 workflows, 20 embedded screenshots + coworker notes under `docs/reference/assets/nt8-2026-07-24/`). bd memory `nt8-windows-reference`. **Residual → bead `aac` (P3):** one more Cowork capture pass (RTH template hours via Tools>Trading Hours, Export/Get-Market-Replay accordion sections, .zip import test, exact Flatten wording) then merge + clear the doc's TODO markers. Low priority; the reference is complete and usable without them.
- **bev (P2, blocked on 8yb — now unblocked on the reference side):** build the repo teaching-agent (`.claude/agents/`) grounded in the NT8 reference + PRAXIS state. **Still waiting on Amear's TEACHING-STYLE PACKAGE** — the bead says do NOT invent the style; bring it and shape the agent around it.
- **Delegation reminder:** VM operator = **Cowork agent** (Claude desktop), self-contained briefs, real Windows paths, sim/backtest-only, **RECOMPUTE hashes (never accept pasted)** — bd memory `cowork-agent-delegation`.
- **cn4 (P2, trader-gated):** Telegram token rotation — untouched, Mac-side, unrelated to the NT8 thread.
- **Hygiene:** AUDIT_LOG.md's one rolling row is by-design (leave for next flush). Orchestrator gate NOT armed session 25; commits plain. Git + dolt pushed at close.

**SESSION-24 RESUME (superseded by session 25 — Cowork agent was running 4uu at close; two teaching-setup tasks queued):**
- **4uu status:** Mac-side prep DONE (NT8-import file `~/praxis-signals/b2-data/NQ-continuous-1min.nt8import.txt` built+validated; brief delivered to the Cowork agent; reconcile = spec-consistency). **VM work was IN PROGRESS at close** (Cowork agent: import → F5 compile → Strategy Analyzer → §B spec-consistency verify). When output lands in `~/praxis-signals/b2-data/inbound/` OR pasted → dispatch the read-only orchestrator-auditor to grade that stage; on PASS, close 4uu → unblocks **zi1 (WFA) → ajj (MC) → xdr (refdist)**. Watch the 09:30 bar-stamp: if shifted, re-stamp ±1 min Mac-side (source stays `NQ-continuous-1min.csv`; regenerate the .nt8import.txt via scratchpad `nt8_import_format.py`).
- **NEW next-chat tasks (Amear-requested, interactive):** **8yb (P2)** — run a research agent to memorize the current NT8 (Windows) layout + PRAXIS workflows → `docs/reference/2026-07-24-nt8-windows-reference.md` (confirm NT8 version first; enrich with Cowork-agent screenshots of the live install). Then **bev (P2, blocked on 8yb)** — build a repo teaching-agent (`.claude/agents/`) that adopts Amear's **TEACHING-STYLE PACKAGE** (he WILL provide it — do NOT invent) grounded in the NT8 reference + PRAXIS state files.
- **Delegation reminder:** VM operator = **Cowork agent** (Claude desktop), self-contained briefs, real Windows paths, sim/backtest-only, **RECOMPUTE hashes (never accept pasted)** — bd memory `cowork-agent-delegation`, runbook `docs/runbooks/2026-07-23-cowork-agent-delegation-brief.md`.
- **Hygiene:** AUDIT_LOG.md's one modified rolling row is STILL by-design (untouched session 24) — leave for the next flush. Orchestrator gate NOT armed this session; commits plain.

**SESSION-23 RESUME (fresh chat — 4uu FILE blocker DISSOLVED; deployed strategy proven current; remaining 4uu is trader-VM hands):**
- **✅ 4uu file-readiness INDEPENDENTLY AUDITED + PASSED (bead 4uu.1 CLOSED):** the VM's 7/17 `PraxisNoiseAreaBreakout.cs` hash "mismatch" was a CRLF line-ending artifact, NOT stale code — proven byte-identical to committed 719ab11 (LF sha256 `35393823e9…2752265` == LF→CRLF sha256 `c22f38fa14…23caa65` == VM's reported hash). Deployed file is safe to F5 as-is. Verified LF copy + `PROVENANCE.txt` on the share at `~/praxis-signals/b2-data/ninjascript/`.
- **🔴 FIRST ACTION — 4uu (P1, TRADER/VM):** F5-compile the (proven-current) `PraxisNoiseAreaBreakout.cs` → import `NQ-continuous-1min.csv` as a custom 1-min series → Strategy Analyzer (RTH 09:30–16:00 ET, ExitMode=BandOrVWAP, Contracts=1, ≥3 mo) → reconcile vs known TV alerts per checklist §B (share copy `b2-data/4uu-reconcile-checklist.md`). Coworker all-clear brief already delivered (clipboard + share). **Top risk UNCHANGED: 09:30 bar-stamp convention** — if NT8 reads the minute stamp as bar-close, band/prevClose shift one bar; check vs NT8 native NQ, re-stamp ±1 min Mac-side if shifted (no fudging in NT8).
- **When compile / Analyzer output lands** (share `b2-data/inbound` OR pasted in chat): dispatch the read-only orchestrator-auditor to independently verify that stage. Re-arm the /loop standing watch if desired — **the Monitor does NOT survive session close**.
- **Then:** hlw closeable (data 44/44, roll pinned D-2026-07-21-A, §4 PASS — trader/VM sign-off call); then **zi1 (WFA) → ajj (MC) → xdr (refdist)**; **cn4** (Telegram token) trader-gated.
- **Hygiene:** AUDIT_LOG.md's single modified rolling row is STILL by-design (untouched session 23) — leave for the next flush. Session 23 made NO code/spec changes; only STATUS/HANDOFF wrap + bead 4uu.1 (dolt). Orchestrator disarmed at close.

**SESSION-22 RESUME (fresh chat — 1u6 tz fix SHIPPED; critical path is now the NT8 re-import, a trader-VM touch):**
- **✅ 1u6 DONE (`f23b648`):** continuous series is now lossless ET (America/New_York), 1,857,362 bars, seam-gap 0, §4 PASS, 09:30-ET anchor verified. On disk at `~/praxis-signals/b2-data/NQ-continuous-1min.csv`. AUDIT_LOG flushed (`9608843`).
- **🔴 FIRST ACTION — 4uu (P1, TRADER/VM):** re-import NQ-continuous-1min.csv into NT8 as a custom 1-min series → compile `ninjascript/PraxisNoiseAreaBreakout.cs` (F5) → load in Strategy Analyzer on the imported series → reconcile sample entries vs known TV alerts (docs/reports/2026-07-16-4uu-noise-area-strategy.md §B). Top risk: bar-timestamp convention (close-stamped RTH 1-min ET assumed). **Trader brief is clipboarded** (scratchpad `trader-reimport-brief.txt`). NO autonomous Mac-side work remains on the critical path until 4uu passes.
- **Then chain:** zi1 (WFA) → ajj (MC) → xdr (refdist), all after 4uu.
- **hlw (P2):** likely closeable — data acquired 44/44, roll pinned (D-2026-07-21-A), cost model done, §4 now PASSES; only a trader/VM sign-off call remains. Confirm + close next attended VM session.
- **cn4 (P2, trader-gated):** Telegram token rotation.
- **Hygiene:** AUDIT_LOG.md flushed this session; exactly ONE rolling row (f8a's own mint) is stranded BY DESIGN — do not "fix" it, it lands in the next flush.

**SESSION-21 RESUME (superseded — 1u6 shipped, critical path advanced to NT8 re-import):**
**SESSION-21 RESUME (fresh chat — data pull DONE 44/44; critical path is now the tz fix):**
- **✅ Exports COMPLETE:** all 44 raw files (22 daily + 22 minute) in `~/praxis-signals/b2-data/raw/`, validated `ready=YES`. Parser (8zd) + daemon OI-gate (pb9) shipped and committed.
- **🔴 FIRST DISPATCH — 1u6 (P2, parked):** the continuous series is UTC, must be America/New_York (09:30-ET-anchored strategy). Attempt-1 introduced silent seam data loss (see bead 1u6 notes for the confirmed root cause + full fix spec: lossless seam re-alignment + hard `distinct_instants(ET)==distinct_instants(UTC baseline)` invariant + seam-gap==0 + a new seam-crossing tz self-test; bar count must return to 1,857,362 or be fully reconciled). Do NOT touch compute_roll_date/build_offsets/back-adjust/OI-guardrail. **Blocked only on the account session-limit reset (~18:20 America/Toronto) — do not retry the quota-dead subagent in that window.**
- **Then 4uu (VM):** once the ET continuous series exists + is re-imported to NT8 — compile PraxisNoiseAreaBreakout.cs + Strategy-Analyzer reconcile. Then the chain **zi1 (WFA) → ajj (MC) → xdr (refdist)**.
- **cn4 (trader-gated):** Telegram token rotation so watcher/signals pings reach the phone.
- **Hygiene:** AUDIT_LOG.md has stranded PASS rows (8zd audits + pb9) → flush per runbook when they accumulate; do not fold into a feature commit.

**SESSION-20 RESUME (superseded — exports now complete 44/44):**
**SESSION-20 RESUME (fresh chat — OI gate CLEARED; critical path is now the data pull):**
- **✅ 6hu DONE (session 20):** volume-only crossover authorized (D-2026-07-21-A). No further action on the OI decision.
- **6bw real-data run (unblocked; waits only on raw/ filling):** `scripts/b2_stitch.py` is BUILT (c2f674d) and volume-only is now authorized. Once the export completes (all 44 files in `~/praxis-signals/b2-data/raw/`), run it with **`--allow-volume-only`**; it emits the stitched continuous series + the spec-§4 validation report (expect the loud VOLUME-ONLY DEVIATION banner — that's intended). Then re-import the continuous series to NT8.
- **Coworker export (in progress):** 6/22 daily, 0/22 1-min at last check. Remaining: 16 daily + 17 chunked minute downloads (2vu) + 22 minute exports. The b2data-watch daemon auto-validates each landing (reports/landing-log.txt). Exports are needed regardless of roll method.
- **4uu (VM, still untouched this session):** compile confirmed earlier; Strategy-Analyzer backtest + TV-alert spot check + bar-timestamp verification remain. Runs after the continuous series exists.
- **cn4 (trader-gated):** Telegram token rotation so watcher/signals pings reach the phone (today they only hit reports/ in the share).
- **9qx (P3, conditional):** add a sanctioned RECONCILED annotate verb + LANDED-RECONCILED flush-verify class — build only if a free-text-mint/message-wording divergence recurs at flush time.
- **Chain after the series lands + validates:** 6bw run → NT8 re-import → **4uu** reconcile → **zi1** WFA → **ajj** MC → **xdr** refdist.

**SESSION-17 RESUME (superseded — trader password DONE, mailbox now relay-free):**
0a. ~~**Trader:** enter NT8 password at the VM login screen~~ — DONE session 18 (VM unlocked, Sim101 flattened).
0b. **Coworker:** finish the pull — the **17 middle contracts 12-21→12-25** in chunked requests (≤~18 months each; 2022/2023/2024/2025 ≈ 4 requests; NEVER the full multi-year span → that triggered the 2vu dialog storm). Then **export all 22 needed contracts to `\\Mac\praxis-signals\b2-data\raw\`**, TWO files each: `NQ-<MMM-YY>_1min.csv` (1-min OHLCV) + `NQ-<MMM-YY>_daily.csv` (daily bars w/ volume+OI). Needed set = 06-21, 09-21, 12-21, 03-22…12-25, 03-26, 06-26, 09-26. On the FIRST daily export, **confirm the Open Interest column has real non-zero values**.
0c. **If OI blank/zero:** STOP — bring the volume-only crossover fallback to the trader; append DECISIONS before proceeding.
0d. **Claude (Mac):** once raw/ is populated, build the Vol/OI-crossover + Difference stitch in Python, validate per b2-data spec §4, re-import the continuous series to NT8 as a custom instrument. Then **4uu** compile+reconcile can run against it; then the chain **zi1 (WFA) → ajj (MC) → xdr (refdist)** proceeds. This part needs no manual VM touch beyond re-import.
0e. Coworker brief for all of the above already delivered (clipboard, session 17).

1. **Trader VM touches (only these gate progress now):** (a) **lhw (P2):** NT8 Control Center → Orders → filter Sim101 → cancel any working SIM-QXD-TEST-0001..0004 (NQ BUY LIMIT @20000, far below market so they sit unfilled); check Positions/flatten strays; 11:12 long 1 NQ + bracket (stop 29741.25 / target 29901.25) still trader's call. (b) **btb sim test:** redeploy the updated ninjascript/PraxisSignalConsumer.cs from repo (post-9d25243), F5-compile in NT8, enable on NQ/Sim101 (market open, outside 17:00-18:00 ET halt), drop tests/fixtures/btb-bad-geometry-long.json → expect Log warning + file in rejected/ + ZERO orders + NO journal line (procedure docs/reports/2026-07-15-btb-oco-geometry-fix.md §71+); on PASS, close btb.
2. **10i activation (trader-gated):** build is done + audited (9905338). To go live: run scripts/praxis-inbound-control-install.sh, launchctl load the LaunchAgent, import + activate workflows/praxis-telegram-inbound-control.json in n8n, (optional) wire the STOP/PAUSE honor-check per report §6 step 4, then text a status command to confirm reply + unauthorized-sender rejection. Steps: docs/reports/2026-07-15-10i-telegram-inbound-build.md.
3. **Block-2 execution (session-15 progress):** **b2-spec-gaps CLOSED** — Q2-Q9 all trader-resolved (DECISIONS.md D-2026-07-15-B: §4 candidate exit checklist, WFE≥0.5 hard gate, NT8 default-provider minute/no-budget, $2.96+1-tick cost, MFFU+trailing-DD RoR basis, optimize-in-WFA 252/63/63, NT8-VM+Python-Mac, fixed SHM bands). **b2-data (hlw) Mac-side landed** (26ce939: spec + config/backtest-cost-model.json + VM runbook); **roll convention LOCKED** (volume/OI-crossover + back-adjusted Difference); hlw half-open on the VM NT8 physical minute pull + §4 validation only. **Session-16 done: ruleset SOURCED (2a3f192) + strategy AUTHORED (719ab11).** 4uu now half-open — Mac-side strategy complete, VM compile+Strategy-Analyzer reconcile remains (see VM queue below). Downstream b2-wfa (zi1, blocked on 4uu+hlw) → b2-mc (ajj) → b2-refdist (xdr — carries Q6 numeric sub-input: MFFU acct size + trailing-DD numbers). **The entire Block-2 chain now waits on the VM data pull (hlw) + strategy compile/reconcile (4uu) — both trader-VM touches.**
   **VM queue for 4uu:** compile ninjascript/PraxisNoiseAreaBreakout.cs in NT8 (F5) → load in Strategy Analyzer on the b2-data NQ 1-min series (roll=vol/OI + Difference) → reconcile sample entry signals vs known live/sim TV alerts (procedure: docs/reports/2026-07-16-4uu-noise-area-strategy.md §B). ⚠️ Top compile risk: bar-timestamp convention (close-stamped RTH 1-min assumed) — if this install stamps differently, band/prevClose shift one bar.
4. Backlog: 518 (P3 monitor-only, VM GUI bug); **6h7 CLOSED de46c56 (session 16)**; VM-side harness cleanup in ~/praxis-signals/VM-CLEANUP-NOTE-2026-07-15.md at next attended VM session; AUDIT_LOG flush when stranded rows accumulate (runbook docs/runbooks/2026-07-10-audit-log-flush.md).

## Recent Decisions
- 2026-05-08 — Git repo initialized at /Volumes/Sensidine/Praxis.build/, pushed to GitHub as amear238/praxis.build (DECISIONS.md#decision-1)
- 2026-05-08 — Local server architecture confirmed over VPS. No cloud API in execution stack. (DECISIONS.md#decision-2)
- 2026-07-04 — D-2026-07-04-A: Build-first reorder. Education no longer a Block 1 prerequisite; comprehension gate moved to pre-live, gating Block 5 (recorded debrief pass/fail).
- 2026-07-04 — D-2026-07-04-B: Strategy Health Monitor added (docs/specs/STRATEGY_HEALTH_MONITOR_SPEC.md). Thresholds locked at Block 2 milestone; built in Block 3; live before Block 5 Phase A.
- 2026-07-08 — D-2026-07-08-A: Coworker visibility via GitHub read access, not Drive sync. Drive-for-Desktop dropped from Step 0.7; Google Sheets tracker retained.
- 2026-07-09 — D-2026-07-09-A: Block 1 tunnel = plain WireGuard (over Tailscale) — no third-party coordination plane, satisfies "no cloud API in execution stack". Applies to B1-a.
- 2026-07-09 — D-2026-07-09-B: NT8 host = Parallels Windows 11 ARM VM now (Apple Silicon = no Boot Camp) / dedicated native x64 Windows PC required before Block 5 live. **NT8 x86-under-emulation now PROVEN VIABLE — B1-0 validation PASS (docs/reports/2026-07-09-b1-0-nt8-parallels-validation.md).** Signals delivered into a folder shared into the VM.
- 2026-07-09 — D-2026-07-09-C: Signals drop dir = dedicated low-priv path (`/Users/admin/praxis-signals` at execution; praxispush user dropped for local-Docker build-first). Structural least-privilege / clean VM-share boundary.
- 2026-07-09 — D-2026-07-09-D: n8n runs LOCALLY on the Mac for Block-1 build-first; public-ingress topology (VPS+WireGuard vs tunnel) DEFERRED to pre-live. Re-scopes B1-a (deferred) + B1-b (local file-write). Keeps D-2026-07-09-A intact for when ingress returns.
- 2026-07-09 — Block 0 milestone — trader sign-off (Amear), 2026-07-09. Independent audit 5 VERIFIED / 3 trader-confirmed / 0 FAIL. Evidence: docs/reports/2026-07-09-block0-milestone-audit.md.
- 2026-07-12 — D-2026-07-12-A: signals relay moved from launchd WatchPaths (10s respawn throttle broke bursts) to persistent KeepAlive 1s-sweep daemon. B1-d's "~4.3s e2e" reclassified as measurement artifact; true unthrottled delivery ~0.5–1.3s.
- 2026-07-15 — D-2026-07-15-B: Block-2 spec gaps Q2-Q9 trader-resolved (§4 candidate exit checklist, WFE≥0.5 hard gate, NT8 default-provider minute/no-budget/~5yr, $2.96 RT + 1-tick cost, MFFU+trailing-DD RoR basis, optimize-in-WFA 252/63/63, NT8-VM+Python-Mac MC, fixed SHM-4/SHM-5 bands). Roll convention LOCKED = volume/OI-crossover + back-adjusted Difference (DECISION_LOG 22:30Z).
- 2026-07-21 — D-2026-07-21-A: OI_BLANK resolved — **volume-only crossover** roll trigger authorized (trader, AskUserQuestion), resolving the D-2026-07-17-A OPEN RISK. NT8 cannot export OI (hard platform limit); Difference back-adjustment UNCHANGED. Enables `scripts/b2_stitch.py --allow-volume-only`. Unblocks 6bw.

## Phase Progress
- [x] Block 0 — Infrastructure Setup (milestone trader-signed-off 2026-07-09)
  - [x] 0.1 Git repo init + GitHub push
  - [x] 0.2 Template files created (CLAUDE.md, STATUS.md, DECISIONS.md, MANIFEST.md, README.md)
  - [x] 0.3 Beads installation + Claude Code hooks
  - [x] 0.4 Google Sheets dashboard
  - [x] 0.5 n8n webhook configuration
  - [x] 0.6 n8n Telegram notification workflow
  - [x] 0.7 Coworker visibility — via GitHub read access (trader confirmed 2026-07-08); Drive sync dropped per D-2026-07-08-A
  - [x] 0.8 Full-loop verification test (verified via remote /tmp/praxis-signals — local delivery is Block 1; see docs/reports/2026-07-08-step-0.8-full-loop.md)
- [x] Block 1 — Foundation (milestone trader-signed-off 2026-07-15; evidence audit docs/reports/2026-07-15-block1-milestone-audit.md, d405125)
  - [x] B1-0 NT8-on-Parallels validation spike (PASS 2026-07-09 — commit 6909b96)
  - [~] B1-a WireGuard tunnel + scoped SSH — DEFERRED per D-2026-07-09-D (n8n local; no remote host). Runbook preserved for pre-live ingress.
  - [x] B1-b n8n LOCAL file-write node (DONE 2026-07-09 — commit b5e022e; atomic .tmp->.json into outbox, 3x/2s retry, Telegram error route)
  - [x] B1-c Mac signals layout + scoped VM share + launchd reconciliation (DONE 2026-07-09 — commit dc5216c; whole-home over-share retired)
  - [x] B1-c-fu stale-heartbeat alert live-fired (7420ae5) · B1-b-fu WatchPaths <5s relay (c19531b)
  - [x] B1-d end-to-end sim latency + idempotency test (PASS 2026-07-10 — d24537c; 4.2-4.3s <5s; findings F1/F2 filed)
  - [x] B1-e offline failure drill (PASS 2026-07-10 — 5822420; spool+replay OK; found F-B1e-1 silent-loss gap — FIXED by stuck-backlog detector, 1c53a18, deployed live)
  - [x] B1-f NinjaScript FileSystemWatcher consumer (DONE 2026-07-15 — source ed2bc9e; T1/T2/T3/T4 all PASS in NT8 sim, standing bracket trader-confirmed in GUI; ct5+9tl closed; evidence docs/reports/2026-07-14-b1f-t1-t3-mac-run.md)
  - [x] **Block 1 MILESTONE — trader-signed-off 2026-07-15 (Amear)** — evidence audit d405125; §6.2 OnTermination-journaling decline RATIFIED (D-2026-07-15-A); known-open carried: btb P2 (next-session dispatch, trader-chosen), 10i P2, 518/01h/qxd P3, 6h7 P4
- [ ] Block 2 — Backtesting (current — entry condition satisfied; planning next session)
- [ ] Block 3 — Circuit Breakers (includes Strategy Health Monitor per D-2026-07-04-B)
- [ ] Block 4 — Paper Trading
- [ ] Block 5 — Graduated Live (comprehension gate: recorded debrief pass required before live — D-2026-07-04-A)
- [ ] Block 6 — Satellite Strategies

## Tooling
- 2026-07-10 — `/progress` Claude Code plugin (praxis-progress@praxis-local, bead jpe) installed user-scope: ASCII progress report toward live SIM from STATUS.md + bd + AUDIT_LOG + git. Report-only; milestones stay human-gated. Source: `plugins/praxis-progress/`; after edits run `claude plugin update praxis-progress`. Spec: docs/specs/2026-07-10-praxis-progress-plugin.md
